diff --git a/core/ff-whisper.js b/core/ff-whisper.js
index 949a70a..7984cfc 100644
--- a/core/ff-whisper.js
+++ b/core/ff-whisper.js
@@ -4,19 +4,32 @@
 (() => {
   'use strict';
   const SR = 16000, NFFT = 400, HOP = 160, NMEL = 80, NFRAMES = 3000, NSAMPLES = 480000;
-  // Two packs: English-only tiny.en (default) and the multilingual tiny model.
+  // Three packs: English-only tiny.en (default), multilingual tiny, and an optional
+  // multilingual small pack (Whisper small, 244M params, int8) for higher accuracy.
   const PACKS = {
-    en: { base: './vendor/models/whisper-tiny-en/', opfs: 'ff-speech-tiny-en', files: [
+    en: { base: './vendor/models/whisper-tiny-en/', opfs: 'ff-speech-tiny-en', heads: 6, files: [
       { name: 'encoder.onnx', sha: '21712ecbe2d1078eaa206b41218a6dff945eb9ac0854b55fd584e8bc88b20368', bytes: 10124977 },
       { name: 'decoder-merged.onnx', parts: ['decoder-merged.onnx.00', 'decoder-merged.onnx.01', 'decoder-merged.onnx.02'], sha: 'c0592d0749413c960569e1c7fb806b060d5d18f3ebad4a95cbf9a77dc6e9be52', bytes: 30718858 },
       { name: 'tokens.json', sha: 'aaaee82ab6816c47c1e361c64e2220a03b8c246fd04e997261b7d0ca6067f8a7', bytes: 508246 }
     ] },
-    multi: { base: './vendor/models/whisper-tiny/', opfs: 'ff-speech-tiny', files: [
+    multi: { base: './vendor/models/whisper-tiny/', opfs: 'ff-speech-tiny', heads: 6, files: [
       { name: 'encoder.onnx', sha: '03ff3c99ce804f79a42afd6212c9492eb75e55625926de66f8fc192e9567d336', bytes: 10124977 },
       { name: 'decoder-merged.onnx', parts: ['decoder-merged.onnx.00', 'decoder-merged.onnx.01', 'decoder-merged.onnx.02'], sha: '25e807a962b6349356d0ea5d0dfe530b7e5bf0e2a484aeca0359d03143faddd3', bytes: 30719241 },
       { name: 'tokens.json', sha: '9c6aae2908bbef3c7ac331e5ed0d7782ba4704b19ca22bb65922d374b018807c', bytes: 565275 }
+    ] },
+    // onnx-community/whisper-small rev 36050c46 (int8). Same tokenizer as tiny, so tokens.json is shared.
+    small: { base: './vendor/models/whisper-small/', opfs: 'ff-speech-small', heads: 12, files: [
+      { name: 'encoder.onnx', parts: ['encoder.onnx.00', 'encoder.onnx.01', 'encoder.onnx.02', 'encoder.onnx.03', 'encoder.onnx.04', 'encoder.onnx.05', 'encoder.onnx.06', 'encoder.onnx.07', 'encoder.onnx.08'], sha: '2601c9eb2d345c5916d4576d36f663a7c96589740fb2273828c48c3fc2c7db75', bytes: 92326127 },
+      { name: 'decoder-merged.onnx', parts: ['decoder-merged.onnx.00', 'decoder-merged.onnx.01', 'decoder-merged.onnx.02', 'decoder-merged.onnx.03', 'decoder-merged.onnx.04', 'decoder-merged.onnx.05', 'decoder-merged.onnx.06', 'decoder-merged.onnx.07', 'decoder-merged.onnx.08', 'decoder-merged.onnx.09', 'decoder-merged.onnx.10', 'decoder-merged.onnx.11', 'decoder-merged.onnx.12', 'decoder-merged.onnx.13', 'decoder-merged.onnx.14', 'decoder-merged.onnx.15'], sha: 'ec07c3cbb64172c39791e26ee870a65ac22b458c36722bfe2776b3dbf741e0c9', bytes: 156750845 },
+      { name: 'tokens.json', path: '../whisper-tiny/tokens.json', sha: '9c6aae2908bbef3c7ac331e5ed0d7782ba4704b19ca22bb65922d374b018807c', bytes: 565275 }
     ] }
   };
+  // Optional word aligner for tiny.en: the Xenova/whisper-tiny.en decoder export (rev 79fb389f,
+  // int8, no cache) that also returns cross-attention weights. One extra forward pass per window
+  // gives real word start times, used for precise word and filler cuts. Loaded only on request.
+  const ALIGNER = { name: 'aligner.onnx', parts: ['aligner.onnx.00', 'aligner.onnx.01', 'aligner.onnx.02'], sha: '62b1761a6c23961cdabbf7caa25495525d78528a64baacf7e4283c83cb2941f1', bytes: 30459718 };
+  const ALIGN_HEADS = [[1, 0], [2, 0], [2, 5], [3, 0], [3, 1], [3, 2], [3, 3], [3, 4]];
+  let aligner = null, aligning = null;
   let packName = 'en', BASE = PACKS.en.base, PACK = PACKS.en.files;
   const live_ = {};
   // " Um, uh, so, like, hmm, I mean, you know," in the model's tokenizer (rev 2575352d).
@@ -45,13 +58,14 @@
     }
     // Large files ship as fixed-size parts (static-host upload limits); the SHA-256 covers the joined file.
     const parts = []; let got = 0;
-    for (const name of f.parts || [f.name]) {
+    for (const name of f.parts || [f.path || f.name]) {
       const res = await fetch(BASE + name, { credentials: 'omit', cache: 'no-cache' });
       if (!res.ok) throw Error(`Speech model file missing (${res.status})`);
       const reader = res.body.getReader();
       for (;;) { const { done, value } = await reader.read(); if (done) break; parts.push(value); got += value.length; onBytes(value.length); if (got > f.bytes) throw Error('Speech model larger than expected'); }
     }
     const buf = new Uint8Array(got); let o = 0; for (const p of parts) { buf.set(p, o); o += p.length; }
+    parts.length = 0; // let the chunk copies go before hashing (matters for the 157 MB decoder)
     if (hex(await crypto.subtle.digest('SHA-256', buf)) !== f.sha) throw Error('Speech model failed its integrity check');
     if (dir) { try { const w = await (await dir.getFileHandle(f.name, { create: true })).createWritable(); await w.write(buf); await w.close(); } catch { /* cache is optional */ } }
     return buf.buffer;
@@ -61,7 +75,7 @@
     for (const f of PACK) { try { if ((await (await dir.getFileHandle(f.name)).getFile()).size !== f.bytes) return false; } catch { return false; } }
     return true;
   }
-  async function forget() { try { await (await navigator.storage.getDirectory()).removeEntry(PACKS[packName].opfs, { recursive: true }); } catch { } enc = dec = vocab = null; loading = null; }
+  async function forget(name = packName) { try { await (await navigator.storage.getDirectory()).removeEntry(PACKS[name].opfs, { recursive: true }); } catch { } if (name === packName) { enc = dec = vocab = null; loading = null; } else delete live_[name]; }
 
   function load(onProgress = () => {}) {
     if (enc && dec && vocab) return Promise.resolve();
@@ -74,7 +88,7 @@
       const opt = { executionProviders: ['wasm'], graphOptimizationLevel: 'all' };
       enc = await ort.InferenceSession.create(new Uint8Array(bufs[0]), opt);
       dec = await ort.InferenceSession.create(new Uint8Array(bufs[1]), opt);
-      if (dir) { try { const keep = new Set(files.map(f => f.name)); for await (const name of dir.keys()) if (!keep.has(name)) await dir.removeEntry(name); } catch { /* best effort */ } }
+      if (dir) { try { const keep = new Set([...files.map(f => f.name), ALIGNER.name]); for await (const name of dir.keys()) if (!keep.has(name)) await dir.removeEntry(name); } catch { /* best effort */ } }
       vocab = JSON.parse(new TextDecoder().decode(bufs[2]));
       vocab.byteOf = byteDecoder();
       vocab.V = vocab.noTimestamps + 1 + 1501; // text + specials, then 1501 timestamp tokens (0.00-30.00 s)
@@ -90,7 +104,8 @@
   }
   function detok(ids) {
     const bytes = []; for (const id of ids) { if (id >= vocab.eos) continue; for (const ch of vocab.tokens[id] || '') { const b = vocab.byteOf.get(ch); if (b !== undefined) bytes.push(b); } }
-    return new TextDecoder().decode(new Uint8Array(bytes)).replace(/\s+/g, ' ').trim();
+    // A window can end mid-character (Devanagari is 3 bytes per letter); drop the broken tail.
+    return new TextDecoder().decode(new Uint8Array(bytes)).replace(/\uFFFD/g, '').replace(/\s+/g, ' ').trim();
   }
 
   // Slaney mel filterbank and periodic Hann window, as defined by the model's training front end.
@@ -151,7 +166,56 @@
     for (const [code, id] of Object.entries(vocab.langs)) { const v = logits[off + id]; if (v > bv) { bv = v; best = code; } }
     return best;
   }
-  async function transcribe(pcm, { maxTokens = 180, timestamps = true, verbatim = false, language = 'auto' } = {}) {
+  function loadAligner() {
+    if (aligner) return Promise.resolve(aligner);
+    if (packName !== 'en') return Promise.reject(Error('Word timing is available for English only'));
+    return aligning ||= (async () => {
+      const buf = await getVerified(ALIGNER, await opfsDir(), () => {});
+      aligner = await ort.InferenceSession.create(new Uint8Array(buf), { executionProviders: ['wasm'], graphOptimizationLevel: 'all' });
+      return aligner;
+    })().catch(err => { aligning = null; throw err; });
+  }
+  // Word start/end times from cross-attention: z-score each alignment head over tokens, median
+  // filter along time, average heads, then a monotonic path (dynamic time warping) through the
+  // token-by-frame matrix. Tokens that begin with a space start a new word.
+  async function alignWords(hidden, textIds, dur) {
+    if (!textIds.length) return [];
+    const s = await loadAligner();
+    const ids = [vocab.sot, vocab.noTimestamps, ...textIds, vocab.eos], L = ids.length, T = Math.max(1, Math.min(1500, Math.round(dur / 0.02)));
+    const out = await s.run({ [s.inputNames[0]]: new ort.Tensor('int64', BigInt64Array.from(ids.map(BigInt)), [1, L]), [s.inputNames[1]]: hidden });
+    const M = new Float32Array(L * T), w = new Float32Array(L * T), win = new Float32Array(7);
+    for (const [layer, h] of ALIGN_HEADS) {
+      const a = out['cross_attentions.' + layer]; if (!a) throw Error('Aligner has no attention output');
+      const F = a.dims[3], d = a.data;
+      for (let i = 0; i < L; i++) for (let t = 0; t < T; t++) w[i * T + t] = d[(h * L + i) * F + t];
+      for (let t = 0; t < T; t++) {
+        let m = 0, v = 0; for (let i = 0; i < L; i++) m += w[i * T + t]; m /= L;
+        for (let i = 0; i < L; i++) v += (w[i * T + t] - m) ** 2; const sd = Math.sqrt(v / L) || 1;
+        for (let i = 0; i < L; i++) w[i * T + t] = (w[i * T + t] - m) / sd;
+      }
+      for (let i = 0; i < L; i++) for (let t = 0; t < T; t++) {
+        for (let j = -3; j <= 3; j++) win[j + 3] = w[i * T + Math.min(T - 1, Math.max(0, t + j))];
+        win.sort(); M[i * T + t] += win[3] / ALIGN_HEADS.length;
+      }
+    }
+    const R = L - 2, W1 = T + 1, C = new Float64Array((R + 1) * W1).fill(Infinity), P = new Uint8Array((R + 1) * W1); C[0] = 0;
+    for (let i = 1; i <= R; i++) for (let t = 1; t <= T; t++) {
+      const diag = C[(i - 1) * W1 + t - 1], up = C[(i - 1) * W1 + t], left = C[i * W1 + t - 1];
+      let m = diag, p = 0; if (up < m) { m = up; p = 1; } if (left < m) { m = left; p = 2; }
+      C[i * W1 + t] = m - M[(i + 1) * T + t - 1]; P[i * W1 + t] = p;
+    }
+    const start = new Int32Array(R).fill(T); let i = R, t = T;
+    while (i > 0 && t > 0) { if (t - 1 < start[i - 1]) start[i - 1] = t - 1; const p = P[i * W1 + t]; if (p === 0) { i--; t--; } else if (p === 1) i--; else t--; }
+    const words = [];
+    textIds.forEach((id, j) => {
+      const bytes = []; for (const ch of vocab.tokens[id] || '') { const b = vocab.byteOf.get(ch); if (b !== undefined) bytes.push(b); }
+      const a = start[j] * 0.02, b = start[j + 1] * 0.02, last = words[words.length - 1];
+      if (!last || bytes[0] === 32) words.push({ bytes, start: a, end: b }); else { last.bytes.push(...bytes); last.end = b; }
+    });
+    return words.map(x => ({ word: new TextDecoder().decode(new Uint8Array(x.bytes)).replace(/\uFFFD/g, '').trim(), start: x.start, end: Math.max(x.start, x.end) })).filter(x => x.word);
+  }
+
+  async function transcribe(pcm, { maxTokens = 180, timestamps = true, verbatim = false, language = 'auto', words: wantWords = false } = {}) {
     await load();
     const t0 = performance.now();
     const feats = new ort.Tensor('float32', logMel(pcm), [1, NMEL, NFRAMES]);
@@ -206,7 +270,9 @@
     }
     if (words.length) { const text = detok(words); if (text) segments.push({ start: cur ? cur.start : (segments.at(-1)?.end ?? 0), end: dur, text }); }
     for (const sg of segments) { sg.start = Math.max(0, Math.min(dur, sg.start)); sg.end = Math.max(sg.start + 0.2, Math.min(dur, sg.end)); }
-    return { text: detok(outIds), language: lang, segments, tokens: outIds.length, encodeMs: Math.round(t1 - t0), totalMs: Math.round(performance.now() - t0) };
+    let aligned = null;
+    if (wantWords && packName === 'en') { try { aligned = await alignWords(hidden, outIds.filter(id => id < vocab.eos), dur); } catch (e) { aligned = null; } }
+    return { text: detok(outIds), language: lang, segments, words: aligned, tokens: outIds.length, encodeMs: Math.round(t1 - t0), totalMs: Math.round(performance.now() - t0) };
   }
 
   // Length to trim when the tail is a repeating token loop: a 1-4 token pattern four or more
@@ -224,7 +290,7 @@
 
   // One decoder step. The merged export keeps a key/value cache between steps,
   // so after the prompt only the newest token is fed; older exports re-run the sequence.
-  const EMPTY = () => new ort.Tensor('float32', new Float32Array(0), [1, 6, 0, 64]);
+  const EMPTY = () => new ort.Tensor('float32', new Float32Array(0), [1, PACKS[packName].heads, 0, 64]);
   async function step_(ids, hidden, cache) {
     const V = vocab.V, cached = dec.inputNames.includes('use_cache_branch');
     if (!cached) {
diff --git a/index.html b/index.html
index 6c70a92..d72a961 100644
--- a/index.html
+++ b/index.html
@@ -1,6 +1,6 @@
 <!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no"><meta name="referrer" content="no-referrer"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval'; worker-src 'self' blob:; style-src 'unsafe-inline'; img-src blob: data:; media-src blob: data:; connect-src 'self'; object-src 'none'; frame-src 'none'; child-src 'none'; manifest-src 'self'; base-uri 'none'; form-action 'none'"><title>FrameForge Studio</title>
 <style>
-:root{--bg:#090a0e;--panel:#11131a;--panel2:#171a22;--line:#292d39;--muted:#8e95a8;--text:#f4f6fb;--accent:#a8ff60;--cyan:#62d9ff;--warn:#ffca5c;--danger:#ff6580;--trackH:64px;--head:92px}*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden;background:var(--bg);color:var(--text);font:13px Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;user-select:none;-webkit-user-select:none}button,input,select{font:inherit}.ico{width:16px;height:16px;display:inline-block;vertical-align:-3px;fill:none;stroke:currentColor;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}.icon .ico{width:17px;height:17px}.trackHead .ico{width:12px;height:12px;vertical-align:-2px}button{color:inherit;background:#20232d;border:1px solid #303541;border-radius:8px;padding:7px 11px;cursor:pointer}button:hover{border-color:#596173;background:#292e3a}button.primary{background:var(--accent);border-color:var(--accent);color:#0a1006;font-weight:750}button.active{background:#394134;border-color:var(--accent);color:var(--accent)}button:disabled{opacity:.45;cursor:not-allowed}.app{height:100%;display:grid;grid-template-rows:52px minmax(220px,1fr) minmax(260px,43vh)}header{display:flex;align-items:center;border-bottom:1px solid var(--line);padding:0 14px;gap:12px;background:#0e1015}.brand{font-weight:850;letter-spacing:-.5px;font-size:17px}.brand i{font-style:normal;color:var(--accent)}.project{color:var(--muted);padding-left:12px;border-left:1px solid var(--line)}.spacer{flex:1}.status{font-size:11px;color:var(--muted)}.status b{color:var(--accent);font-weight:650}.workspace{display:grid;grid-template-columns:236px minmax(0,1fr) 250px;min-height:0}.side{background:var(--panel);border-right:1px solid var(--line);min-height:0;overflow:auto}.right{border-right:0;border-left:1px solid var(--line)}.tabs{display:flex;border-bottom:1px solid var(--line)}.tabs button{flex:1;background:none;border:0;border-radius:0;color:var(--muted);padding:12px 4px}.tabs button.active{color:var(--text);box-shadow:inset 0 -2px var(--accent)}.pad{padding:12px}.drop{border:1px dashed #424858;border-radius:12px;padding:18px 8px;text-align:center;color:var(--muted);background:#151820}.drop strong{display:block;color:var(--text);margin-bottom:5px}.fxPreset{width:100%;margin:5px 0;text-align:left}.autoCap{padding:12px;margin:0 0 4px;border:1px solid #29303a;border-radius:9px;background:#0d1014}.autoCap b{display:block;margin-bottom:4px}.autoCap small{display:block;color:#9ba4b5;line-height:1.45;margin-bottom:10px}.autoCapStatus{font-size:12px;color:#9ba4b5;margin-top:8px}.autoCapStatus:empty{display:none}#transcript:empty{display:none}#transcript{margin-top:12px;padding-top:12px;border-top:1px solid #29303a}.txHead b{display:block}.txHead small{display:block;color:#9ba4b5;margin:3px 0 8px}.capLangRow{display:flex;align-items:center;gap:10px;margin:2px 0 10px}.capLangRow span{color:var(--muted,#9aa3b2);font-size:13px;white-space:nowrap}.capLangRow select{flex:1;min-height:36px;font:inherit;background:#20232d;color:var(--text);border:1px solid #303541;border-radius:8px;padding:0 10px}.txTools{display:flex;gap:6px;margin-bottom:8px}.txTools input{flex:1;min-width:0;min-height:36px;font:inherit;background:#20232d;color:var(--text);border:1px solid #303541;border-radius:8px;padding:0 10px}.txTools input:focus{outline:2px solid #628ae0;outline-offset:1px}.txTools button{flex:1;min-height:36px;white-space:nowrap}.tw.hit{background:#4a3d12;border-color:#8a7020;color:#ffe9a8}.txWords{display:flex;flex-wrap:wrap;gap:4px}.tw{padding:6px 8px;min-height:32px;border-radius:6px;background:#171a22;border:1px solid #29303a;font-size:13px;line-height:1.1}.tw.cut{text-decoration:line-through;color:#ff8da1;border-color:#ff658066;background:#2a1419}.scopeBox{margin-top:14px;padding:10px;background:#0a0c10;border:1px solid #29303a;border-radius:9px}.scopeBox b,.scopeBox small{display:block;color:#9ba4b5;margin-bottom:7px}.scopeBox canvas{width:100%;height:96px;background:#050607;box-shadow:none}.captionClip{background:linear-gradient(135deg,#286245,#3b7e61)!important;border-color:#67d9a0!important}.titlePreset{margin-top:10px;padding:12px;border:1px solid #303541;border-radius:9px;background:#191c24}.titlePreset small{display:block;color:var(--muted);margin-top:4px}.library{display:grid;gap:8px;margin-top:10px}.captionList{display:grid;gap:6px;margin-top:12px}.captionRow{display:grid;grid-template-columns:42px 1fr;gap:7px;text-align:left;padding:7px;background:#171b23;border:1px solid #2c323e;border-radius:7px}.captionRow.active{border-color:var(--accent);background:#243021}.captionRow time{color:var(--accent);font-size:10px}.captionRow span{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;font-size:11px}.asset{padding:8px;border-radius:9px;background:#1a1d26;display:grid;grid-template-columns:48px 1fr;gap:8px;align-items:center}.thumb{height:34px;border-radius:6px;background:linear-gradient(135deg,#3e6d8d,#8c4f7f);display:grid;place-items:center;font-size:16px}.asset small{display:block;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.viewer{background:#07080b;display:grid;grid-template-rows:minmax(0,1fr) 47px;min-width:0}.stageWrap{position:relative;display:grid;place-items:center;min-height:0;overflow:hidden;background:radial-gradient(circle at 50% 45%,#1c202a 0,#08090d 68%)}canvas{display:block;max-width:100%;max-height:100%;aspect-ratio:16/9;background:#000;box-shadow:0 12px 40px #0009}.safe{position:absolute;border:1px dashed #fff3;width:80%;height:80%;pointer-events:none;display:none}.captionOverlay{position:fixed;z-index:20;border:1px solid var(--accent);box-shadow:0 0 0 1px #000;pointer-events:none;display:none}.captionOverlay.show{display:block}.captionOverlay:before{content:"CAPTION";position:absolute;left:0;top:-18px;background:var(--accent);color:#071006;font-size:9px;font-weight:800;padding:2px 5px}.captionResize{position:absolute;right:-7px;top:50%;width:14px;height:30px;transform:translateY(-50%);background:var(--accent);border:2px solid #071006;border-radius:5px;pointer-events:auto;cursor:ew-resize;touch-action:none}.controls{display:flex;justify-content:center;align-items:center;gap:10px;border-top:1px solid var(--line);background:#101218}.time{font-variant-numeric:tabular-nums;min-width:98px;color:#c9ced9}.icon{min-width:34px;padding:6px}.inspector h3{display:flex;justify-content:space-between;align-items:center;margin:4px 0 16px;font-size:12px;text-transform:uppercase;letter-spacing:1px;color:#aab1c1}.field{margin:13px 0}.field label{display:flex;justify-content:space-between;color:#aab1c1;margin-bottom:7px}.field output{color:var(--text);font-variant-numeric:tabular-nums}.field input[type=range]{width:100%;accent-color:var(--accent)}.field select{width:100%;background:#20232d;color:var(--text);border:1px solid #303541;border-radius:8px;padding:8px}.empty{color:var(--muted);line-height:1.5}.timeline{min-width:0;width:100%;overflow:hidden;min-height:0;background:#0d0f14;border-top:1px solid var(--line);display:grid;grid-template-rows:42px 28px minmax(0,1fr)}.toolbar{display:flex;align-items:center;padding:0 10px;gap:7px;border-bottom:1px solid var(--line)}.toolbar .zoom{width:100px}.ruler{position:relative;margin-left:var(--head);overflow:hidden;background:#12141b;border-bottom:1px solid var(--line);font-size:10px;color:#767e91}.tick{position:absolute;top:6px;border-left:1px solid #414654;padding-left:4px}.tracksScroll{overflow:auto;position:relative;min-width:0;width:100%}.tracks{position:relative;min-width:100%;padding-bottom:18px}.track{height:var(--trackH);display:flex;border-bottom:1px solid #242833;position:relative}.trackHead{position:sticky;left:0;z-index:8;width:var(--head);min-width:var(--head);background:#151820;border-right:1px solid #303440;padding:8px;color:#bdc3d0}.trackHead small{display:block;color:#6f7789;margin-top:3px}.lane{height:100%;position:relative;min-width:calc(100% - var(--head));background:repeating-linear-gradient(90deg,transparent 0,transparent 99px,#1b1e27 100px)}.clip{position:absolute;top:6px;height:52px;border:1px solid #478fb0;background:linear-gradient(135deg,#255e78,#313f65);border-radius:7px;overflow:hidden;color:#fff;box-shadow:0 2px 8px #0006;touch-action:none}.clip.audio{background:linear-gradient(135deg,#59407c,#773e70);border-color:#a45aa2}.clip.selected{outline:2px solid var(--accent);outline-offset:1px}.clip .label{padding:6px 8px;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.clip .waves{height:25px;opacity:.62;background:repeating-linear-gradient(90deg,transparent 0 3px,#fff 4px 5px,transparent 6px 8px);clip-path:polygon(0 45%,3% 20%,6% 70%,9% 35%,12% 80%,15% 15%,18% 62%,22% 40%,25% 74%,28% 31%,31% 60%,35% 22%,38% 72%,41% 38%,44% 83%,47% 20%,51% 67%,55% 39%,59% 75%,63% 32%,67% 65%,71% 19%,75% 77%,79% 35%,83% 61%,87% 28%,91% 72%,95% 44%,100% 55%,100% 60%,0 60%)}.realWave{height:25px;display:flex;gap:2px;align-items:center;padding:0 6px;opacity:.7}.realWave i{display:block;width:2px;background:#fff;border-radius:2px}.handle{position:absolute;top:0;width:9px;height:100%;cursor:ew-resize;background:#ffffff18}.handle.l{left:0}.handle.r{right:0}.playhead{position:absolute;top:0;bottom:0;width:1px;background:#ff5e72;z-index:7;pointer-events:none;margin-left:var(--head)}.playhead:before{content:"";position:absolute;top:0;left:-5px;border:6px solid transparent;border-top:9px solid #ff5e72}.trackHead button{padding:1px 4px;font-size:9px;margin-left:3px}.mixerPanel{display:none;position:fixed;z-index:60;right:18px;top:60px;width:430px;max-height:calc(100vh - 80px);overflow:auto;background:#14171e;border:1px solid #39404c;border-radius:13px;box-shadow:0 18px 70px #000;padding:14px}.mixerPanel.show{display:block}.mixerHead{display:flex;justify-content:space-between;align-items:center;font-size:15px;margin-bottom:12px}.channels{display:flex;gap:10px}.channel{flex:1;min-width:110px;background:#0c0e13;border:1px solid #282d37;border-radius:9px;padding:9px}.channel label,.master label{display:flex;justify-content:space-between;color:#aeb5c4}.channel input,.master input{width:100%;accent-color:var(--accent)}.meter{height:8px;background:#292d36;border-radius:6px;overflow:hidden;margin-top:8px}.meter i{display:block;width:0;height:100%;background:linear-gradient(90deg,#63ef79,#ffd052,#ff6077);transition:.08s}.master{margin-top:12px;padding-top:12px;border-top:1px solid #303541}@media(max-width:620px){.mixerPanel{inset:52px 8px 8px;width:auto}.channels{overflow-x:auto}}.toast{position:fixed;left:50%;bottom:22px;transform:translateX(-50%);background:#20242e;border:1px solid #39404e;padding:10px 15px;border-radius:9px;box-shadow:0 8px 32px #000b;z-index:100;opacity:0;pointer-events:none;transition:.2s}.toast.show{opacity:1}.exportModal{position:fixed;inset:0;background:#000b;z-index:90;display:none;place-items:center}.exportModal.show{display:grid}.card{width:min(440px,calc(100vw - 28px));background:#171a22;border:1px solid #373c49;border-radius:16px;padding:20px;box-shadow:0 24px 80px #000}.progress{height:8px;background:#282c36;border-radius:9px;overflow:hidden;margin:16px 0}.progress i{display:block;height:100%;width:0;background:var(--accent);transition:.15s}.row{display:flex;gap:8px;align-items:center}.kbd{color:#8e95a8;font-size:11px}.curveBox{border-top:1px solid var(--line);margin-top:15px;padding-top:13px}.curveBox label{display:flex;justify-content:space-between;align-items:center;color:#aab1c1}.curveBox select{width:auto!important;padding:4px!important}.curveBox svg{width:100%;height:112px;margin:9px 0;background:#0b0d12;border:1px solid #2b303b;border-radius:8px;touch-action:none}.curveGrid{stroke:#252a34;stroke-width:1}.axisLabel{fill:#737c90;font-size:8px;font-family:system-ui}.axisLine{stroke:#4a5060;stroke-width:1}.curveLine{fill:none;stroke:var(--accent);stroke-width:2}.curvePoint{fill:#ffd052;stroke:#151820;stroke-width:2;cursor:pointer}.curvePoint.sel{fill:#ff7189;stroke:#fff}.tangent{stroke:#ffc85c;stroke-width:1}.tangentHandle{fill:#ffc85c;stroke:#171a22;stroke-width:2;cursor:grab}.tangentHandle.in{fill:#62d9ff}.marquee{fill:#62d9ff22;stroke:#62d9ff;stroke-dasharray:4 3}.tangentTools{margin-top:7px}.tangentTools button{flex:1;font-size:11px;padding:5px}.curveHint{display:block;color:#737c90;line-height:1.4;margin-top:8px}#closeInspector{display:none;padding:1px 7px;font-size:20px}.keybtn{padding:0 3px;border:0;background:none;color:#8790a2}.keybtn.active{color:var(--warn);background:none}.keydot{position:absolute;bottom:3px;width:7px;height:7px;background:var(--warn);transform:rotate(45deg);z-index:4}.marker{position:absolute;top:0;bottom:0;width:1px;background:#ffd05288;z-index:6;pointer-events:none}.rangeZone{position:absolute;top:0;bottom:0;background:#5b8cff12;border-left:2px solid #62d9ff;border-right:2px solid #ffca5c;z-index:3;pointer-events:none}.rangeFlag{position:absolute;top:0;z-index:8;font-size:9px;font-weight:800;padding:2px 4px;color:#071006;pointer-events:none}.rangeFlag.in{background:#62d9ff}.rangeFlag.out{background:#ffca5c}.marker:before{content:"◆";position:absolute;top:2px;left:-6px;color:#ffd052;font-size:10px}#inspectBtn{display:none}.badge{font-size:10px;border:1px solid #3b4352;border-radius:20px;padding:3px 6px;color:#9fa7b7}
+:root{--bg:#090a0e;--panel:#11131a;--panel2:#171a22;--line:#292d39;--muted:#8e95a8;--text:#f4f6fb;--accent:#a8ff60;--cyan:#62d9ff;--warn:#ffca5c;--danger:#ff6580;--trackH:64px;--head:92px}*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden;background:var(--bg);color:var(--text);font:13px Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;user-select:none;-webkit-user-select:none}button,input,select{font:inherit}.ico{width:16px;height:16px;display:inline-block;vertical-align:-3px;fill:none;stroke:currentColor;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}.icon .ico{width:17px;height:17px}.trackHead .ico{width:12px;height:12px;vertical-align:-2px}button{color:inherit;background:#20232d;border:1px solid #303541;border-radius:8px;padding:7px 11px;cursor:pointer}button:hover{border-color:#596173;background:#292e3a}button.primary{background:var(--accent);border-color:var(--accent);color:#0a1006;font-weight:750}button.active{background:#394134;border-color:var(--accent);color:var(--accent)}button:disabled{opacity:.45;cursor:not-allowed}.app{height:100%;display:grid;grid-template-rows:52px minmax(220px,1fr) minmax(260px,43vh)}header{display:flex;align-items:center;border-bottom:1px solid var(--line);padding:0 14px;gap:12px;background:#0e1015}.brand{font-weight:850;letter-spacing:-.5px;font-size:17px}.brand i{font-style:normal;color:var(--accent)}.project{color:var(--muted);padding-left:12px;border-left:1px solid var(--line)}.spacer{flex:1}.status{font-size:11px;color:var(--muted)}.status b{color:var(--accent);font-weight:650}.workspace{display:grid;grid-template-columns:236px minmax(0,1fr) 250px;min-height:0}.side{background:var(--panel);border-right:1px solid var(--line);min-height:0;overflow:auto}.right{border-right:0;border-left:1px solid var(--line)}.tabs{display:flex;border-bottom:1px solid var(--line)}.tabs button{flex:1;background:none;border:0;border-radius:0;color:var(--muted);padding:12px 4px}.tabs button.active{color:var(--text);box-shadow:inset 0 -2px var(--accent)}.pad{padding:12px}.drop{border:1px dashed #424858;border-radius:12px;padding:18px 8px;text-align:center;color:var(--muted);background:#151820}.drop strong{display:block;color:var(--text);margin-bottom:5px}.fxPreset{width:100%;margin:5px 0;text-align:left}.autoCap{padding:12px;margin:0 0 4px;border:1px solid #29303a;border-radius:9px;background:#0d1014}.autoCap b{display:block;margin-bottom:4px}.autoCap small{display:block;color:#9ba4b5;line-height:1.45;margin-bottom:10px}.autoCapStatus{font-size:12px;color:#9ba4b5;margin-top:8px}.autoCapStatus:empty{display:none}#transcript:empty{display:none}#transcript{margin-top:12px;padding-top:12px;border-top:1px solid #29303a}.txHead b{display:block}.txHead small{display:block;color:#9ba4b5;margin:3px 0 8px}.capLangRow{display:flex;align-items:center;gap:10px;margin:2px 0 10px}.capLangRow span{color:var(--muted,#9aa3b2);font-size:13px;white-space:nowrap;min-width:64px}.capCheck{display:flex;align-items:center;gap:10px;min-height:40px;margin:-2px 0 8px;font-size:13px;color:#c9d0dc;cursor:pointer}.capCheck input{width:20px;height:20px;accent-color:#628ae0;flex:none;margin:0}.capCheck[hidden]{display:none}.capRemove{display:block;background:none;border:0;color:#9ba4b5;text-decoration:underline;font:inherit;font-size:12px;padding:0;min-height:32px;margin:-4px 0 6px;cursor:pointer}.capLangRow[hidden],.capRemove[hidden]{display:none}.capLangRow select{flex:1;min-height:36px;font:inherit;background:#20232d;color:var(--text);border:1px solid #303541;border-radius:8px;padding:0 10px}.txTools{display:flex;gap:6px;margin-bottom:8px}.txTools input{flex:1;min-width:0;min-height:36px;font:inherit;background:#20232d;color:var(--text);border:1px solid #303541;border-radius:8px;padding:0 10px}.txTools input:focus{outline:2px solid #628ae0;outline-offset:1px}.txTools button{flex:1;min-height:36px;white-space:nowrap}.tw.hit{background:#4a3d12;border-color:#8a7020;color:#ffe9a8}.txWords{display:flex;flex-wrap:wrap;gap:4px}.tw{padding:6px 8px;min-height:32px;border-radius:6px;background:#171a22;border:1px solid #29303a;font-size:13px;line-height:1.1}.tw.cut{text-decoration:line-through;color:#ff8da1;border-color:#ff658066;background:#2a1419}.scopeBox{margin-top:14px;padding:10px;background:#0a0c10;border:1px solid #29303a;border-radius:9px}.scopeBox b,.scopeBox small{display:block;color:#9ba4b5;margin-bottom:7px}.scopeBox canvas{width:100%;height:96px;background:#050607;box-shadow:none}.captionClip{background:linear-gradient(135deg,#286245,#3b7e61)!important;border-color:#67d9a0!important}.titlePreset{margin-top:10px;padding:12px;border:1px solid #303541;border-radius:9px;background:#191c24}.titlePreset small{display:block;color:var(--muted);margin-top:4px}.library{display:grid;gap:8px;margin-top:10px}.captionList{display:grid;gap:6px;margin-top:12px}.captionRow{display:grid;grid-template-columns:42px 1fr;gap:7px;text-align:left;padding:7px;background:#171b23;border:1px solid #2c323e;border-radius:7px}.captionRow.active{border-color:var(--accent);background:#243021}.captionRow time{color:var(--accent);font-size:10px}.captionRow span{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;font-size:11px}.asset{padding:8px;border-radius:9px;background:#1a1d26;display:grid;grid-template-columns:48px 1fr;gap:8px;align-items:center}.thumb{height:34px;border-radius:6px;background:linear-gradient(135deg,#3e6d8d,#8c4f7f);display:grid;place-items:center;font-size:16px}.asset small{display:block;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.viewer{background:#07080b;display:grid;grid-template-rows:minmax(0,1fr) 47px;min-width:0}.stageWrap{position:relative;display:grid;place-items:center;min-height:0;overflow:hidden;background:radial-gradient(circle at 50% 45%,#1c202a 0,#08090d 68%)}canvas{display:block;max-width:100%;max-height:100%;aspect-ratio:16/9;background:#000;box-shadow:0 12px 40px #0009}.safe{position:absolute;border:1px dashed #fff3;width:80%;height:80%;pointer-events:none;display:none}.captionOverlay{position:fixed;z-index:20;border:1px solid var(--accent);box-shadow:0 0 0 1px #000;pointer-events:none;display:none}.captionOverlay.show{display:block}.captionOverlay:before{content:"CAPTION";position:absolute;left:0;top:-18px;background:var(--accent);color:#071006;font-size:9px;font-weight:800;padding:2px 5px}.captionResize{position:absolute;right:-7px;top:50%;width:14px;height:30px;transform:translateY(-50%);background:var(--accent);border:2px solid #071006;border-radius:5px;pointer-events:auto;cursor:ew-resize;touch-action:none}.controls{display:flex;justify-content:center;align-items:center;gap:10px;border-top:1px solid var(--line);background:#101218}.time{font-variant-numeric:tabular-nums;min-width:98px;color:#c9ced9}.icon{min-width:34px;padding:6px}.inspector h3{display:flex;justify-content:space-between;align-items:center;margin:4px 0 16px;font-size:12px;text-transform:uppercase;letter-spacing:1px;color:#aab1c1}.field{margin:13px 0}.field label{display:flex;justify-content:space-between;color:#aab1c1;margin-bottom:7px}.field output{color:var(--text);font-variant-numeric:tabular-nums}.field input[type=range]{width:100%;accent-color:var(--accent)}.field select{width:100%;background:#20232d;color:var(--text);border:1px solid #303541;border-radius:8px;padding:8px}.empty{color:var(--muted);line-height:1.5}.timeline{min-width:0;width:100%;overflow:hidden;min-height:0;background:#0d0f14;border-top:1px solid var(--line);display:grid;grid-template-rows:42px 28px minmax(0,1fr)}.toolbar{display:flex;align-items:center;padding:0 10px;gap:7px;border-bottom:1px solid var(--line)}.toolbar .zoom{width:100px}.ruler{position:relative;margin-left:var(--head);overflow:hidden;background:#12141b;border-bottom:1px solid var(--line);font-size:10px;color:#767e91}.tick{position:absolute;top:6px;border-left:1px solid #414654;padding-left:4px}.tracksScroll{overflow:auto;position:relative;min-width:0;width:100%}.tracks{position:relative;min-width:100%;padding-bottom:18px}.track{height:var(--trackH);display:flex;border-bottom:1px solid #242833;position:relative}.trackHead{position:sticky;left:0;z-index:8;width:var(--head);min-width:var(--head);background:#151820;border-right:1px solid #303440;padding:8px;color:#bdc3d0}.trackHead small{display:block;color:#6f7789;margin-top:3px}.lane{height:100%;position:relative;min-width:calc(100% - var(--head));background:repeating-linear-gradient(90deg,transparent 0,transparent 99px,#1b1e27 100px)}.clip{position:absolute;top:6px;height:52px;border:1px solid #478fb0;background:linear-gradient(135deg,#255e78,#313f65);border-radius:7px;overflow:hidden;color:#fff;box-shadow:0 2px 8px #0006;touch-action:none}.clip.audio{background:linear-gradient(135deg,#59407c,#773e70);border-color:#a45aa2}.clip.selected{outline:2px solid var(--accent);outline-offset:1px}.clip .label{padding:6px 8px;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.clip .waves{height:25px;opacity:.62;background:repeating-linear-gradient(90deg,transparent 0 3px,#fff 4px 5px,transparent 6px 8px);clip-path:polygon(0 45%,3% 20%,6% 70%,9% 35%,12% 80%,15% 15%,18% 62%,22% 40%,25% 74%,28% 31%,31% 60%,35% 22%,38% 72%,41% 38%,44% 83%,47% 20%,51% 67%,55% 39%,59% 75%,63% 32%,67% 65%,71% 19%,75% 77%,79% 35%,83% 61%,87% 28%,91% 72%,95% 44%,100% 55%,100% 60%,0 60%)}.realWave{height:25px;display:flex;gap:2px;align-items:center;padding:0 6px;opacity:.7}.realWave i{display:block;width:2px;background:#fff;border-radius:2px}.handle{position:absolute;top:0;width:9px;height:100%;cursor:ew-resize;background:#ffffff18}.handle.l{left:0}.handle.r{right:0}.playhead{position:absolute;top:0;bottom:0;width:1px;background:#ff5e72;z-index:7;pointer-events:none;margin-left:var(--head)}.playhead:before{content:"";position:absolute;top:0;left:-5px;border:6px solid transparent;border-top:9px solid #ff5e72}.trackHead button{padding:1px 4px;font-size:9px;margin-left:3px}.mixerPanel{display:none;position:fixed;z-index:60;right:18px;top:60px;width:430px;max-height:calc(100vh - 80px);overflow:auto;background:#14171e;border:1px solid #39404c;border-radius:13px;box-shadow:0 18px 70px #000;padding:14px}.mixerPanel.show{display:block}.mixerHead{display:flex;justify-content:space-between;align-items:center;font-size:15px;margin-bottom:12px}.channels{display:flex;gap:10px}.channel{flex:1;min-width:110px;background:#0c0e13;border:1px solid #282d37;border-radius:9px;padding:9px}.channel label,.master label{display:flex;justify-content:space-between;color:#aeb5c4}.channel input,.master input{width:100%;accent-color:var(--accent)}.meter{height:8px;background:#292d36;border-radius:6px;overflow:hidden;margin-top:8px}.meter i{display:block;width:0;height:100%;background:linear-gradient(90deg,#63ef79,#ffd052,#ff6077);transition:.08s}.master{margin-top:12px;padding-top:12px;border-top:1px solid #303541}@media(max-width:620px){.mixerPanel{inset:52px 8px 8px;width:auto}.channels{overflow-x:auto}}.toast{position:fixed;left:50%;bottom:22px;transform:translateX(-50%);background:#20242e;border:1px solid #39404e;padding:10px 15px;border-radius:9px;box-shadow:0 8px 32px #000b;z-index:100;opacity:0;pointer-events:none;transition:.2s}.toast.show{opacity:1}.exportModal{position:fixed;inset:0;background:#000b;z-index:90;display:none;place-items:center}.exportModal.show{display:grid}.card{width:min(440px,calc(100vw - 28px));background:#171a22;border:1px solid #373c49;border-radius:16px;padding:20px;box-shadow:0 24px 80px #000}.progress{height:8px;background:#282c36;border-radius:9px;overflow:hidden;margin:16px 0}.progress i{display:block;height:100%;width:0;background:var(--accent);transition:.15s}.row{display:flex;gap:8px;align-items:center}.kbd{color:#8e95a8;font-size:11px}.curveBox{border-top:1px solid var(--line);margin-top:15px;padding-top:13px}.curveBox label{display:flex;justify-content:space-between;align-items:center;color:#aab1c1}.curveBox select{width:auto!important;padding:4px!important}.curveBox svg{width:100%;height:112px;margin:9px 0;background:#0b0d12;border:1px solid #2b303b;border-radius:8px;touch-action:none}.curveGrid{stroke:#252a34;stroke-width:1}.axisLabel{fill:#737c90;font-size:8px;font-family:system-ui}.axisLine{stroke:#4a5060;stroke-width:1}.curveLine{fill:none;stroke:var(--accent);stroke-width:2}.curvePoint{fill:#ffd052;stroke:#151820;stroke-width:2;cursor:pointer}.curvePoint.sel{fill:#ff7189;stroke:#fff}.tangent{stroke:#ffc85c;stroke-width:1}.tangentHandle{fill:#ffc85c;stroke:#171a22;stroke-width:2;cursor:grab}.tangentHandle.in{fill:#62d9ff}.marquee{fill:#62d9ff22;stroke:#62d9ff;stroke-dasharray:4 3}.tangentTools{margin-top:7px}.tangentTools button{flex:1;font-size:11px;padding:5px}.curveHint{display:block;color:#737c90;line-height:1.4;margin-top:8px}#closeInspector{display:none;padding:1px 7px;font-size:20px}.keybtn{padding:0 3px;border:0;background:none;color:#8790a2}.keybtn.active{color:var(--warn);background:none}.keydot{position:absolute;bottom:3px;width:7px;height:7px;background:var(--warn);transform:rotate(45deg);z-index:4}.marker{position:absolute;top:0;bottom:0;width:1px;background:#ffd05288;z-index:6;pointer-events:none}.rangeZone{position:absolute;top:0;bottom:0;background:#5b8cff12;border-left:2px solid #62d9ff;border-right:2px solid #ffca5c;z-index:3;pointer-events:none}.rangeFlag{position:absolute;top:0;z-index:8;font-size:9px;font-weight:800;padding:2px 4px;color:#071006;pointer-events:none}.rangeFlag.in{background:#62d9ff}.rangeFlag.out{background:#ffca5c}.marker:before{content:"◆";position:absolute;top:2px;left:-6px;color:#ffd052;font-size:10px}#inspectBtn{display:none}.badge{font-size:10px;border:1px solid #3b4352;border-radius:20px;padding:3px 6px;color:#9fa7b7}
 @media(max-width:900px){.workspace{grid-template-columns:minmax(0,1fr) 220px}.side:not(.right){display:none}.app{grid-template-rows:50px minmax(200px,48vh) 1fr}.viewer{grid-column:1}.project,.status{display:none}.right{grid-column:2}.timeline{min-height:220px}.clip{height:50px}}
 @media(max-width:620px){.right.drawer{display:block;position:fixed;z-index:50;right:0;top:48px;bottom:0;width:min(310px,88vw);box-shadow:-18px 0 50px #000;background:#11131a}.right.drawer .pad{padding-bottom:70px} .right.drawer #closeInspector{display:block}#inspectBtn{display:block}.workspace{display:block;position:relative}.right{display:none}.app{grid-template-rows:48px 39vh 1fr}.viewer{height:100%}header{padding:0 8px;gap:6px}header button{padding:6px 8px}.brand{font-size:15px}.controls{justify-content:space-around}.kbd{display:none}:root{--head:72px;--trackH:58px}.trackHead{font-size:11px;padding:7px}.clip{height:46px}.timeline{grid-template-rows:40px 25px minmax(0,1fr)}.toolbar{overflow-x:auto}.toolbar button{white-space:nowrap}.zoom{display:none}}
 
@@ -59,7 +59,7 @@ button{transition:background .14s cubic-bezier(.2,.8,.2,1),border-color .14s,col
 <symbol id="i-cut" viewBox="0 0 24 24"><circle cx="6" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="m8.5 7.5 11 7M8.5 16.5 19.5 9"/></symbol><symbol id="i-ripple" viewBox="0 0 24 24"><path d="M4 5v14M9 12h11M13 8l-4 4 4 4"/></symbol><symbol id="i-copy" viewBox="0 0 24 24"><rect x="8" y="8" width="11" height="11" rx="2"/><path d="M16 8V5a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h3"/></symbol><symbol id="i-marker" viewBox="0 0 24 24"><path d="m12 3 7 9-7 9-7-9z"/></symbol><symbol id="i-track" viewBox="0 0 24 24"><path d="M4 7h16M4 17h16M8 3v8M16 13v8"/></symbol><symbol id="i-magnet" viewBox="0 0 24 24"><path d="M5 4v9a7 7 0 0 0 14 0V4h-5v9a2 2 0 0 1-4 0V4z"/><path d="M5 8h5M14 8h5"/></symbol><symbol id="i-lock" viewBox="0 0 24 24"><rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/></symbol><symbol id="i-unlock" viewBox="0 0 24 24"><rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 7-2"/></symbol><symbol id="i-plus" viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></symbol>
 </defs></svg><div class="app">
 <header><div class="brand"><span class="brandMark" aria-hidden="true"><b></b><b></b></span><i>Frame</i>Forge</div><div class="project">Untitled film <span class="badge">LOCAL-FIRST</span></div><div class="spacer"></div><div class="status"><b>●</b> Autosaved locally</div><button id="undo" title="Undo"><svg class="ico" aria-hidden="true"><use href="#i-undo"/></svg></button><button id="redo" title="Redo"><svg class="ico" aria-hidden="true"><use href="#i-redo"/></svg></button><button id="projectSettingsBtn">Project</button><button id="mixerBtn">Mixer</button><button id="inspectBtn">Inspect</button><button id="vaultBtn" title="Encrypt this project at rest">Encrypt</button><button id="saveBtn">Save</button><button class="primary" id="exportBtn">Export</button></header>
-<section class="workspace"><aside class="side"><div class="tabs"><button id="mediaTab" class="active">Media</button><button id="textTab">Text</button><button id="fxTab">FX</button></div><div class="pad" id="mediaPanel"><div class="drop" id="drop"><strong>Drop footage here</strong><span>MP4, WebM, MOV, audio</span><br><button style="margin-top:10px" id="importBtn">Choose files</button><input id="file" type="file" accept="video/*,audio/*,image/*" multiple hidden></div><button id="demoBtn" style="width:100%;margin-top:10px">Load demo project</button><div class="mediaTools"><input id="mediaSearch" maxlength="120" placeholder="Search media" aria-label="Search media"><select id="mediaKindFilter" aria-label="Filter media"><option value="all">All</option><option value="video">Video</option><option value="audio">Audio</option><option value="image">Images</option><option value="text">Graphics</option></select></div><div class="mediaStats" id="mediaStats"></div><div class="library" id="library"></div></div><div class="pad" id="textPanel" hidden><h3>Titles</h3><p class="empty">Original vector titles rendered directly in the compositor.</p><button id="addTitle" class="primary" style="width:100%">＋ Add title</button><div class="titlePreset" id="editorialPreset"><b>Editorial</b><small>Clean lower-third motion</small></div><div class="titlePreset" id="impactPreset"><b>Impact</b><small>Large centered headline</small></div><hr style="border:0;border-top:1px solid #303541;margin:16px 0"><h3>Captions</h3><div class="autoCap"><b>Auto captions</b><small id="autoCapNote">Transcribes the selected clip on this device. Nothing is uploaded.</small><label class="capLangRow"><span>Language</span><select id="capLang" aria-label="Caption language"><option value="en">English</option><option value="auto">Detect automatically</option><option value="hi">Hindi</option><option value="es">Spanish</option><option value="fr">French</option><option value="de">German</option><option value="pt">Portuguese</option><option value="it">Italian</option><option value="ru">Russian</option><option value="ja">Japanese</option><option value="ko">Korean</option><option value="zh">Chinese</option><option value="ar">Arabic</option><option value="bn">Bengali</option><option value="ta">Tamil</option><option value="te">Telugu</option><option value="mr">Marathi</option><option value="ur">Urdu</option><option value="tr">Turkish</option><option value="nl">Dutch</option><option value="pl">Polish</option><option value="id">Indonesian</option><option value="vi">Vietnamese</option></select></label><button id="autoCaptions" class="primary" style="width:100%">Transcribe selected clip</button><div id="autoCapStatus" class="autoCapStatus" aria-live="polite"></div><div id="transcript"></div></div><button id="addCaption" style="width:100%;margin-top:10px">＋ Caption at playhead</button><button id="importSrt" style="width:100%;margin-top:7px">Import SRT / VTT</button><button id="exportSrt" style="width:100%;margin-top:7px">Export SRT</button><button id="exportVtt" style="width:100%;margin-top:7px">Export WebVTT</button><input id="srtFile" type="file" accept=".srt,.vtt,text/plain,text/vtt" hidden><input id="captionSearch" maxlength="120" placeholder="Search captions" style="width:100%;margin-top:12px;background:#20232d;color:#fff;border:1px solid #303541;border-radius:8px;padding:8px"><div class="row" style="margin-top:7px"><input id="captionReplace" maxlength="2000" placeholder="Replace with" style="min-width:0;flex:1;background:#20232d;color:#fff;border:1px solid #303541;border-radius:8px;padding:8px"><button id="captionWhole" title="Match whole words only">Word</button><button id="replaceCaption" title="Replace in selected cue">1</button><button id="replaceCaptions" title="Replace in every matching cue">All</button></div><small id="replacePreview" style="display:block;color:#8e95a8;margin-top:7px;line-height:1.35">Enter a search to preview replacements</small><div class="row" id="replaceSelectTools" style="margin-top:5px;display:none"><button id="replaceSelectAll" style="font-size:10px;padding:3px 6px">Select all</button><button id="replaceSelectNone" style="font-size:10px;padding:3px 6px">None</button><button id="replaceInvert" style="font-size:10px;padding:3px 6px">Invert</button><button id="replaceShowAll" style="font-size:10px;padding:3px 6px">Show all</button></div><div id="replaceDiff" style="display:grid;gap:4px;margin-top:6px"></div><div class="row" style="margin-top:6px"><button id="clearReplaceHistory" style="display:none;font-size:9px;padding:2px 5px">Clear history</button></div><div id="replaceHistory" style="margin-top:4px"></div><div class="captionList" id="captionList"></div></div><div class="pad" id="fxPanel" hidden><h3>Color & Effects</h3><p class="empty">Looks and LUTs apply to the selected clip. Preview uses whichever grader is faster on this device; export always grades in the verified core.</p><button class="fxPreset" data-fx="clean">Clean contrast</button><button class="fxPreset" data-fx="film">Warm film</button><button class="fxPreset" data-fx="night">Night grade</button><button class="fxPreset" data-fx="bleach">Bleach bypass</button><div class="scopeBox"><b>Live luma scope</b><canvas id="scope" width="210" height="96"></canvas><small id="scopeReadout" style="display:block;margin-top:8px">Move playhead onto a clip</small></div></div></aside>
+<section class="workspace"><aside class="side"><div class="tabs"><button id="mediaTab" class="active">Media</button><button id="textTab">Text</button><button id="fxTab">FX</button></div><div class="pad" id="mediaPanel"><div class="drop" id="drop"><strong>Drop footage here</strong><span>MP4, WebM, MOV, audio</span><br><button style="margin-top:10px" id="importBtn">Choose files</button><input id="file" type="file" accept="video/*,audio/*,image/*" multiple hidden></div><button id="demoBtn" style="width:100%;margin-top:10px">Load demo project</button><div class="mediaTools"><input id="mediaSearch" maxlength="120" placeholder="Search media" aria-label="Search media"><select id="mediaKindFilter" aria-label="Filter media"><option value="all">All</option><option value="video">Video</option><option value="audio">Audio</option><option value="image">Images</option><option value="text">Graphics</option></select></div><div class="mediaStats" id="mediaStats"></div><div class="library" id="library"></div></div><div class="pad" id="textPanel" hidden><h3>Titles</h3><p class="empty">Original vector titles rendered directly in the compositor.</p><button id="addTitle" class="primary" style="width:100%">＋ Add title</button><div class="titlePreset" id="editorialPreset"><b>Editorial</b><small>Clean lower-third motion</small></div><div class="titlePreset" id="impactPreset"><b>Impact</b><small>Large centered headline</small></div><hr style="border:0;border-top:1px solid #303541;margin:16px 0"><h3>Captions</h3><div class="autoCap"><b>Auto captions</b><small id="autoCapNote">Transcribes the selected clip on this device. Nothing is uploaded.</small><label class="capLangRow"><span>Language</span><select id="capLang" aria-label="Caption language"><option value="en">English</option><option value="auto">Detect automatically</option><option value="hi">Hindi</option><option value="es">Spanish</option><option value="fr">French</option><option value="de">German</option><option value="pt">Portuguese</option><option value="it">Italian</option><option value="ru">Russian</option><option value="ja">Japanese</option><option value="ko">Korean</option><option value="zh">Chinese</option><option value="ar">Arabic</option><option value="bn">Bengali</option><option value="ta">Tamil</option><option value="te">Telugu</option><option value="mr">Marathi</option><option value="ur">Urdu</option><option value="tr">Turkish</option><option value="nl">Dutch</option><option value="pl">Polish</option><option value="id">Indonesian</option><option value="vi">Vietnamese</option></select></label><label class="capLangRow" id="capModelRow" hidden><span>Model</span><select id="capModel" aria-label="Caption model"><option value="multi">Standard · 39 MB</option><option value="small">High accuracy · 238 MB</option></select></label><label class="capCheck" id="capFillRow"><input type="checkbox" id="capFillers"><span>Keep ums and uhs, with exact word timing for cuts (one-time 30 MB)</span></label><button type="button" id="capModelRemove" class="capRemove" hidden>Remove high-accuracy model from this device</button><button id="autoCaptions" class="primary" style="width:100%">Transcribe selected clip</button><div id="autoCapStatus" class="autoCapStatus" aria-live="polite"></div><div id="transcript"></div></div><button id="addCaption" style="width:100%;margin-top:10px">＋ Caption at playhead</button><button id="importSrt" style="width:100%;margin-top:7px">Import SRT / VTT</button><button id="exportSrt" style="width:100%;margin-top:7px">Export SRT</button><button id="exportVtt" style="width:100%;margin-top:7px">Export WebVTT</button><input id="srtFile" type="file" accept=".srt,.vtt,text/plain,text/vtt" hidden><input id="captionSearch" maxlength="120" placeholder="Search captions" style="width:100%;margin-top:12px;background:#20232d;color:#fff;border:1px solid #303541;border-radius:8px;padding:8px"><div class="row" style="margin-top:7px"><input id="captionReplace" maxlength="2000" placeholder="Replace with" style="min-width:0;flex:1;background:#20232d;color:#fff;border:1px solid #303541;border-radius:8px;padding:8px"><button id="captionWhole" title="Match whole words only">Word</button><button id="replaceCaption" title="Replace in selected cue">1</button><button id="replaceCaptions" title="Replace in every matching cue">All</button></div><small id="replacePreview" style="display:block;color:#8e95a8;margin-top:7px;line-height:1.35">Enter a search to preview replacements</small><div class="row" id="replaceSelectTools" style="margin-top:5px;display:none"><button id="replaceSelectAll" style="font-size:10px;padding:3px 6px">Select all</button><button id="replaceSelectNone" style="font-size:10px;padding:3px 6px">None</button><button id="replaceInvert" style="font-size:10px;padding:3px 6px">Invert</button><button id="replaceShowAll" style="font-size:10px;padding:3px 6px">Show all</button></div><div id="replaceDiff" style="display:grid;gap:4px;margin-top:6px"></div><div class="row" style="margin-top:6px"><button id="clearReplaceHistory" style="display:none;font-size:9px;padding:2px 5px">Clear history</button></div><div id="replaceHistory" style="margin-top:4px"></div><div class="captionList" id="captionList"></div></div><div class="pad" id="fxPanel" hidden><h3>Color & Effects</h3><p class="empty">Looks and LUTs apply to the selected clip. Preview uses whichever grader is faster on this device; export always grades in the verified core.</p><button class="fxPreset" data-fx="clean">Clean contrast</button><button class="fxPreset" data-fx="film">Warm film</button><button class="fxPreset" data-fx="night">Night grade</button><button class="fxPreset" data-fx="bleach">Bleach bypass</button><div class="scopeBox"><b>Live luma scope</b><canvas id="scope" width="210" height="96"></canvas><small id="scopeReadout" style="display:block;margin-top:8px">Move playhead onto a clip</small></div></div></aside>
 <main class="viewer"><div class="stageWrap"><canvas id="preview" width="1280" height="720"></canvas><div class="safe"></div><div id="captionOverlay" class="captionOverlay"><i id="captionResize" class="captionResize"></i></div></div><div class="controls"><button class="icon" id="back" title="Go to start"><svg class="ico" aria-hidden="true"><use href="#i-prev"/></svg></button><button class="icon" id="rev" title="Reverse shuttle (J)"><svg class="ico" aria-hidden="true"><use href="#i-rev"/></svg></button><button class="icon" id="play" title="Play/pause (K / Space)"><svg class="ico" aria-hidden="true"><use href="#i-play"/></svg></button><button class="icon" id="fwd" title="Forward shuttle (L)"><svg class="ico" aria-hidden="true"><use href="#i-fast"/></svg></button><button class="icon" id="next" title="Go to end"><svg class="ico" aria-hidden="true"><use href="#i-next"/></svg></button><button id="setIn" title="Set range In (I)">I</button><button id="setOut" title="Set range Out (O)">O</button><button id="loop" title="Loop In/Out range"><svg class="ico" aria-hidden="true"><use href="#i-loop"/></svg></button><div class="time"><span id="tc">00:00:00</span> / <span id="dur">00:00:00</span></div><select id="speed"><option value="0.5">0.5×</option><option selected value="1">1×</option><option value="1.5">1.5×</option><option value="2">2×</option></select></div></main>
 <aside class="side right"><div class="pad inspector"><h3>Inspector <button id="closeInspector" aria-label="Close inspector">×</button></h3><div id="empty" class="empty">Select a clip to adjust transform, sound, speed and look.</div><div id="fields" hidden><div class="field"><label>Opacity <span><button class="keybtn" data-prop="opacity">◇</button> <output id="opacityV">100%</output></span></label><input id="opacity" type="range" min="0" max="100" value="100"></div><div class="field"><label>Volume <span><button class="keybtn" data-prop="volume">◇</button> <output id="volumeV">100%</output></span></label><input id="volume" type="range" min="0" max="200" value="100"></div><div class="field"><label>Speed <output id="clipSpeedV">1.0×</output></label><input id="clipSpeed" type="range" min="25" max="400" value="100"></div><div class="field trimTools"><label>Trim <span class="trimHint">[ ] slip · Alt roll · Ctrl slide</span></label><div class="trimRow"><div class="trimModes" role="radiogroup" aria-label="Trim mode"><button data-trim-mode="slip" role="radio" aria-checked="true" class="on">Slip</button><button data-trim-mode="roll" role="radio" aria-checked="false">Roll</button><button data-trim-mode="slide" role="radio" aria-checked="false">Slide</button></div><button class="trimStep" data-trim-step="-1" aria-label="Trim back one frame">‹</button><button class="trimStep" data-trim-step="1" aria-label="Trim forward one frame">›</button></div></div><div class="field"><label>Scale <span><button class="keybtn" data-prop="scale">◇</button> <output id="scaleV">100%</output></span></label><input id="scale" type="range" min="25" max="200" value="100"></div><div class="field"><label>Position X <span><button class="keybtn" data-prop="x">◇</button> <output id="xV">0%</output></span></label><input id="x" type="range" min="-100" max="100" value="0"></div><div class="field"><label>Position Y <span><button class="keybtn" data-prop="y">◇</button> <output id="yV">0%</output></span></label><input id="y" type="range" min="-100" max="100" value="0"></div><div class="field"><label>Audio pan <span><button class="keybtn" data-prop="pan">◇</button> <output id="panV">C</output></span></label><input id="pan" type="range" min="-100" max="100" value="0"></div><div id="captionFields" hidden><div class="field"><label>Caption text</label><textarea id="captionText" maxlength="10000" rows="3" style="width:100%;background:#20232d;color:#fff;border:1px solid #303541;border-radius:8px;padding:8px"></textarea></div><div class="field"><label>Position</label><select id="captionPos"><option value="bottom">Bottom</option><option value="middle">Middle</option><option value="top">Top</option></select></div><div class="field"><label>Horizontal <output id="captionXV">0%</output></label><input id="captionX" type="range" min="-40" max="40" value="0"></div><div class="field"><label>Vertical <output id="captionYV">34%</output></label><input id="captionY" type="range" min="-40" max="40" value="34"></div><small class="curveHint">Tip: drag the selected caption directly in the preview.</small><div class="field"><label>Text region <output id="captionWidthV">80%</output></label><input id="captionWidth" type="range" min="30" max="95" value="80"></div><div class="field"><label>Line spacing <output id="captionLeadingV">120%</output></label><input id="captionLeading" type="range" min="80" max="180" value="120"></div><div class="field"><label>Fade in <output id="captionFadeInV">0.20s</output></label><input id="captionFadeIn" type="range" min="0" max="150" value="20"></div><div class="field"><label>Fade out <output id="captionFadeOutV">0.20s</output></label><input id="captionFadeOut" type="range" min="0" max="150" value="20"></div><div class="field"><label>Fade easing</label><select id="captionEase"><option value="linear">Linear</option><option value="smooth" selected>Smooth</option><option value="cinematic">Cinematic</option></select></div><div class="field"><label>Karaoke highlight</label><select id="captionKaraoke"><option value="off" selected>Off</option><option value="word">Word by word</option><option value="progress">Progressive</option></select></div><div class="field"><label>Highlight color</label><input id="captionHighlight" type="color" value="#ffca5c" style="width:100%;height:32px"></div><div class="field"><label>Word cue points (%)</label><textarea id="captionTimings" maxlength="5000" rows="2" placeholder="0, 18, 35, 52" style="width:100%;background:#20232d;color:#fff;border:1px solid #303541;border-radius:8px;padding:8px"></textarea><small class="curveHint">One start percentage per word. Leave blank for even timing.</small><button id="applyCaptionTimings" style="width:100%;margin-top:6px">Set custom word timing</button></div><div class="field"><label>Size <output id="captionSizeV">42px</output></label><input id="captionSize" type="range" min="24" max="72" value="42"></div><div class="field"><label>Font</label><select id="captionFont"><option value="system-ui">Sans</option><option value="Georgia">Serif</option><option value="monospace">Mono</option></select></div><div class="field"><label>Weight</label><select id="captionWeight"><option value="500">Medium</option><option value="700" selected>Bold</option><option value="900">Heavy</option></select></div><div class="field"><label>Text align</label><select id="captionAlign"><option value="left">Left</option><option value="center" selected>Center</option><option value="right">Right</option></select></div><div class="field"><label>Text color</label><input id="captionColor" type="color" value="#ffffff" style="width:100%;height:32px"></div><div class="field"><label>Outline <output id="captionStrokeV">5px</output></label><input id="captionStroke" type="range" min="0" max="12" value="5"></div><div class="field"><label>Background <output id="captionBgV">75%</output></label><input id="captionBg" type="range" min="0" max="100" value="75"></div><button id="applyCaptionStyle" style="width:100%">Apply style to all captions</button></div><div id="titleFields" hidden><div class="field"><label>Title text</label><textarea id="titleText" maxlength="2000" rows="3" style="width:100%;resize:vertical;background:#20232d;color:#fff;border:1px solid #303541;border-radius:8px;padding:8px">Your story</textarea></div><div class="field"><label>Font size <output id="fontSizeV">64px</output></label><input id="fontSize" type="range" min="24" max="160" value="64"></div><div class="field"><label>Color</label><input id="titleColor" type="color" value="#ffffff" style="width:100%;height:32px"></div><div class="field"><label>Alignment</label><select id="titleAlign"><option value="left">Left</option><option value="center" selected>Center</option><option value="right">Right</option></select></div><div class="field"><label>Stroke <output id="strokeV">0px</output></label><input id="strokeWidth" type="range" min="0" max="16" value="0"></div><div class="field"><label>Background <output id="bgV">0%</output></label><input id="bgOpacity" type="range" min="0" max="100" value="0"></div></div><div class="field"><label>Exposure <output id="exposureV">0.0</output></label><input id="exposure" type="range" min="-200" max="200" value="0"></div><div class="field"><label>Contrast <output id="contrastV">100%</output></label><input id="contrast" type="range" min="50" max="200" value="100"></div><div class="field"><label>Saturation <output id="saturationV">100%</output></label><input id="saturation" type="range" min="0" max="200" value="100"></div><div class="field"><label>Temperature <output id="temperatureV">0</output></label><input id="temperature" type="range" min="-100" max="100" value="0"></div><div class="field"><label>Look</label><select id="filter"><option value="none">Original</option><option value="cinema">Cinema</option><option value="mono">Monochrome</option><option value="warm">Warm film</option><option value="cool">Cool night</option></select></div><div class="field"><label>3D LUT <output id="lutV">None</output></label><input id="lutFile" type="file" accept=".cube" hidden><button id="loadLut" style="width:100%">Load .cube locally</button></div><div class="field"><label>Transition</label><select id="transition"><option value="none">Cut</option><option value="fade">Cross dissolve</option><option value="dip">Dip to black</option><option value="swivel">Swivel teaser</option></select></div><div class="curveBox"><label>Keyframe curve <select id="curveProp"><option value="opacity">Opacity</option><option value="scale">Scale</option><option value="x">Position X</option><option value="y">Position Y</option><option value="volume">Volume</option><option value="pan">Pan</option></select></label><svg id="curve" viewBox="0 0 220 105" aria-label="Keyframe curve editor"></svg><div class="row"><button id="prevKey">◁ Key</button><button id="nextKey">Key ▷</button><button id="deleteKey" style="color:#ff8798">Delete key(s)</button></div><div class="field"><label>Interpolation</label><select id="easeMode"><option value="smooth">Bezier smooth</option><option value="linear">Linear</option><option value="hold">Hold</option></select></div><div class="row tangentTools"><button id="tangentMode" class="active">⛓ Linked tangents</button><button id="fitCurve">Fit curve</button></div><small class="curveHint">Shift-click or drag a marquee to multi-select. Drag amber handles to shape bezier easing.</small></div><button id="delete" style="width:100%;color:#ff8798;margin-top:10px">Delete clip</button></div></div></aside></section>
 <section class="timeline"><div class="toolbar"><button id="split"><svg class="ico" aria-hidden="true"><use href="#i-cut"/></svg> Split <span class="kbd">S</span></button><button id="ripple"><svg class="ico" aria-hidden="true"><use href="#i-ripple"/></svg> Ripple delete</button><button id="duplicate"><svg class="ico" aria-hidden="true"><use href="#i-copy"/></svg> Duplicate</button><button id="marker"><svg class="ico" aria-hidden="true"><use href="#i-marker"/></svg> Marker <span class="kbd">M</span></button><button id="autoCutBtn" title="Local silence and restart-marker edits">Auto Cut</button><button id="addTrack"><svg class="ico" aria-hidden="true"><use href="#i-track"/></svg> Track</button><button id="snap" class="active"><svg class="ico" aria-hidden="true"><use href="#i-magnet"/></svg> Snap</button><div class="spacer"></div><button id="fitTimeline" title="Fit full timeline">Fit</button><span class="kbd">Zoom</span><input class="zoom" id="zoom" type="range" min="35" max="180" value="80"><span class="precisionHelp">ALT bypass · , . nudge</span></div><div class="ruler" id="ruler"></div><div class="tracksScroll" id="scroll"><div class="tracks" id="tracks"></div><div class="playhead" id="head"></div></div></section></div>
@@ -156,7 +156,7 @@ function remove(){let c=mutableSelected();if(!c)return;snapshot();for(const t of
 async function doExport(){if(exporting)return;if(!state.tracks.some(t=>t.clips.length))return toast('Add clips before export');if(!window.MediaRecorder||!canvas.captureStream)return toast('This browser cannot export WebM');let D=duration(),mime=['video/webm;codecs=vp9','video/webm;codecs=vp8','video/webm'].find(x=>MediaRecorder.isTypeSupported(x));if(!Number.isFinite(D)||D<=0)return toast('Timeline duration is invalid');if(!mime)return toast('No supported WebM encoder found');exporting=true;setPlaying(false);$('#modal').classList.add('show');$('#exportText').textContent='FrameForge is compositing locally. Keep this tab open until the download starts.';$('#bar').style.width='0';$('#pct').textContent='0%';let stream=canvas.captureStream(30),exportAC=null,audioDest=null,audioSources=[];try{exportAC=new (window.AudioContext||window.webkitAudioContext)();audioDest=exportAC.createMediaStreamDestination();for(const tr of state.tracks)for(const c of tr.clips){let b=audioBuffers.get(c.assetId),tr=state.tracks.find(t=>t.clips.includes(c)),soloed=state.tracks.some(t=>t.type==='audio'&&t.solo);if(!b||tr?.mute||(soloed&&!tr?.solo))continue;let src=exportAC.createBufferSource(),clipGain=exportAC.createGain(),clipPan=exportAC.createStereoPanner?exportAC.createStereoPanner():null,chain=audioChain(exportAC,tr,audioDest);src.buffer=b;src.playbackRate.value=c.speed;clipGain.gain.value=Math.max(0,Math.min(2,c.volume));for(const k of (c.keyframes?.volume||[]))clipGain.gain.linearRampToValueAtTime(Math.max(0,Math.min(2,k.v)),exportAC.currentTime+c.start+k.t);src.connect(clipGain);if(clipPan){clipPan.pan.value=Math.max(-1,Math.min(1,c.pan||0));for(const k of (c.keyframes?.pan||[]))clipPan.pan.linearRampToValueAtTime(Math.max(-1,Math.min(1,k.v)),exportAC.currentTime+c.start+k.t);clipGain.connect(clipPan).connect(chain.input)}else clipGain.connect(chain.input);src.start(exportAC.currentTime+c.start,Math.max(0,c.in),Math.min(c.duration,b.duration-Math.max(0,c.in)));audioSources.push(src)}for(const at of audioDest.stream.getAudioTracks())stream.addTrack(at)}catch(e){}let audioTrackCount=stream.getAudioTracks().length,rec,parts=[],exportError=null;try{rec=new MediaRecorder(stream,{mimeType:mime,videoBitsPerSecond:6000000})}catch(e){exporting=false;if(exportAC)exportAC.close();stream.getTracks().forEach(t=>t.stop());$('#exportText').textContent='Export could not start in this browser.';return}rec.ondataavailable=e=>e.data.size&&parts.push(e.data);let resolve;const done=new Promise(r=>resolve=r);rec.onstop=resolve;rec.onerror=e=>{exportError=e.error||new Error('Recorder error');resolve()};rec.start(200);let t0=performance.now(),last=0;async function frame(now){if(!exporting){rec.stop();return}let t=(now-t0)/1000;if(t>=D-.02){draw(D,true);rec.requestData();setTimeout(()=>{if(rec.state!=='inactive')rec.stop()},80);return}if(t-last>.02){for(const {c} of activeAt(t)){let v=vids.get(c.assetId);if(v&&Math.abs(v.currentTime-((t-c.start)*c.speed+c.in))>.12)v.currentTime=Math.max(0,(t-c.start)*c.speed+c.in)}draw(t,true);last=t;let p=Math.round(t/D*100);$('#bar').style.width=p+'%';$('#pct').textContent=p+'%'}setTimeout(()=>frame(performance.now()),33)}setTimeout(()=>frame(performance.now()),0);let watchdog=setTimeout(()=>{try{if(rec.state!=='inactive'){exportError=new Error('Export timed out');rec.requestData();rec.stop()}}catch(e){exportError=e}},Math.max(10000,D*8000));await done;clearTimeout(watchdog);if(exporting&&parts.length){let blob=new Blob(parts,{type:mime}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='frameforge-export.webm';a.click();$('#exportText').textContent=`Export complete · ${(blob.size/1048576).toFixed(1)} MB WebM`;$('#bar').style.width='100%';$('#pct').textContent='100%';setTimeout(()=>$('#modal').classList.remove('show'),1000);window.__lastExport={size:blob.size,type:mime,duration:D,audioTracks:audioTrackCount,ok:true}}else{window.__lastExport={size:0,type:mime,duration:D,audioTracks:audioTrackCount,ok:false,error:exportError?.message||'No encoded data'};$('#exportText').textContent='Export failed before a playable file was created.';$('#pct').textContent='Failed'}if(exportAC)exportAC.close();stream.getTracks().forEach(t=>t.stop());exporting=false;current=0;renderAll()}
 function formatSpec(){return {'16:9':[1920,1080],'9:16':[1080,1920],'1:1':[1080,1080],'4:5':[1080,1350]}[projectAspect]||[1920,1080]}
 function applyProjectFormat(notify=true){let [w,h]=formatSpec(),scale=Math.min(1,1280/w);canvas.width=Math.round(w*scale);canvas.height=Math.round(h*scale);canvas.style.aspectRatio=`${w}/${h}`;$('#projectAspect').value=projectAspect;$('#projectFps').value=projectFps;$('#projectFormatReadout').textContent=`${w} × ${h} · ${projectFps} fps`;canvas.classList.remove('formatPulse');void canvas.offsetWidth;canvas.classList.add('formatPulse');draw(current);renderTimeline();queueSave();if(notify)toast(`${projectAspect} · ${projectFps} fps applied`)}
-function mobileView(mode){if(innerWidth>620)return;document.querySelector('.side:not(.right)').classList.toggle('mobileOpen',mode==='media');document.querySelector('.right').classList.toggle('drawer',mode==='inspect');document.querySelectorAll('.mobileDock button').forEach(b=>b.classList.toggle('active',b.dataset.mobileView===mode));if(mode==='preview')document.querySelector('.viewer').scrollIntoView({block:'start'});if(mode==='edit')document.querySelector('.timeline').scrollIntoView({block:'start'})}$('#projectSettingsBtn').onclick=()=>{$('#projectSettingsPanel').classList.add('show');$('#projectAspect').value=projectAspect;$('#projectFps').value=projectFps};$('#closeProjectSettings').onclick=()=>$('#projectSettingsPanel').classList.remove('show');$('#applyProjectSettings').onclick=()=>{projectAspect=$('#projectAspect').value;projectFps=+$('#projectFps').value;applyProjectFormat();$('#projectSettingsPanel').classList.remove('show')};document.querySelectorAll('[data-mobile-view]').forEach(b=>b.onclick=()=>mobileView(b.dataset.mobileView));let autoCutPlan=null;$('#autoCutBtn').onclick=()=>{$('#autoCutPanel').classList.add('show')};$('#closeAutoCut').onclick=()=>$('#autoCutPanel').classList.remove('show');$('#vadThreshold').oninput=e=>$('#vadThresholdV').value=(+e.target.value/100).toFixed(2);$('#vadPadding').oninput=e=>$('#vadPaddingV').value=e.target.value+' ms';$('#voicePreset').onclick=()=>{let c=mutableSelected();if(!c)return;let tr=state.tracks.find(t=>t.clips.includes(c));if(tr?.type!=='audio')return toast('Select a voice audio clip');let p=voicePolishSpec();tr.voicePolish=p;tr.lowEQ=0;tr.highEQ=3;tr.compressor=35;renderMixer();queueSave();toast('Voice polish chain set')};$('#analyzeAutoCut').onclick=analyzeAutoCut;$('#applyAutoCut').onclick=()=>applyKeepPlan(autoCutPlan);$('#mixerBtn').onclick=()=>{$('#mixerPanel').classList.add('show');renderMixer()};$('#closeMixer').onclick=()=>$('#mixerPanel').classList.remove('show');$('#master').onpointerdown=()=>snapshot();$('#master').oninput=e=>{state.masterGain=dbToGain(+e.target.value);updateLiveAll();$('#masterV').value=(+e.target.value).toFixed(1)+' dB';$('#masterMeter').style.width=Math.min(100,state.masterGain*62)+'%';queueSave()};$('#mediaSearch').oninput=e=>{mediaQuery=e.target.value;renderLibrary()};$('#mediaKindFilter').onchange=e=>{mediaFilter=e.target.value;renderLibrary()};$('#fitTimeline').onclick=fitTimeline;$('#mediaTab').onclick=()=>{$('#mediaPanel').hidden=false;$('#textPanel').hidden=true;$('#fxPanel').hidden=true;$('#mediaTab').classList.add('active');$('#textTab').classList.remove('active')};let captionWhole=false,replacementExcluded=new Set(),replaceShowAll=false,replaceHistory=[];try{let h=JSON.parse(localStorage.getItem('ff-replace-history')||'[]');replaceHistory=Array.isArray(h)?h.slice(0,5):[]}catch(e){localStorage.removeItem('ff-replace-history')}$('#clearReplaceHistory').onclick=()=>{replaceHistory=[];localStorage.setItem('ff-replace-history','[]');renderReplaceHistory();$('#clearReplaceHistory').style.display='none';toast('Replacement history cleared')};$('#captionSearch').oninput=()=>{replacementExcluded.clear();replaceShowAll=false;renderCaptionList()};$('#captionReplace').oninput=replacePreview;$('#replaceSelectAll').onclick=()=>{replacementExcluded.clear();replacePreview()};$('#replaceSelectNone').onclick=()=>{state.tracks.flatMap(t=>t.clips).filter(c=>c.asset?.type==='caption').forEach(c=>replacementExcluded.add(c.id));replacePreview()};$('#replaceInvert').onclick=()=>{document.querySelectorAll('[data-replace-cue]').forEach(x=>x.checked?replacementExcluded.add(x.dataset.replaceCue):replacementExcluded.delete(x.dataset.replaceCue));replacePreview()};$('#replaceShowAll').onclick=()=>{replaceShowAll=!replaceShowAll;replacePreview()};$('#captionWhole').onclick=e=>{captionWhole=!captionWhole;e.currentTarget.classList.toggle('active',captionWhole);e.currentTarget.textContent=captionWhole?'Word ✓':'Word';replacePreview()};$('#replaceCaption').onclick=()=>replaceCaptionText(false);$('#replaceCaptions').onclick=()=>replaceCaptionText(true);$('#textTab').onclick=()=>{$('#mediaPanel').hidden=true;$('#textPanel').hidden=false;$('#fxPanel').hidden=true;$('#textTab').classList.add('active');$('#mediaTab').classList.remove('active');$('#fxTab').classList.remove('active')};$('#fxTab').onclick=()=>{$('#mediaPanel').hidden=true;$('#textPanel').hidden=true;$('#fxPanel').hidden=false;$('#fxTab').classList.add('active');$('#mediaTab').classList.remove('active');$('#textTab').classList.remove('active');requestAnimationFrame(()=>updateScope(true))};let autoCapBusy=false,autoCapArmed=false;const CJK=/[\u3040-\u30ff\u3400-\u9fff\uf900-\ufaff\uac00-\ud7af]/;function cjkGroups(text,maxChars=16){let parts=text.replace(/\s+/g,'').split(/(?<=[。！？、，,.!?])/).filter(Boolean),groups=[],cur='';for(let p of parts){while(p.length>maxChars){if(cur){groups.push(cur);cur=''}groups.push(p.slice(0,maxChars));p=p.slice(maxChars)}if((cur+p).length>maxChars&&cur){groups.push(cur);cur=''}cur+=p}if(cur)groups.push(cur);return groups.reduce((o,g)=>(/^[。！？、，,.!?]+$/.test(g)&&o.length?o[o.length-1]+=g:o.push(g),o),[])}function captionChunks(text,a,b,maxWords=7){let w=text.split(/\s+/).filter(Boolean);if(!w.length)return [];let groups=[];if(CJK.test(text)&&text.replace(/\s+/g,'').length>16&&w.length<4)groups=cjkGroups(text);else{let n=Math.ceil(w.length/maxWords),per=Math.ceil(w.length/n);for(let i=0;i<w.length;i+=per)groups.push(w.slice(i,i+per).join(' '))}let total=groups.reduce((n,g)=>n+g.length,0)||1,t=a,out=[];for(let g of groups){let d=(b-a)*g.length/total;out.push([g,t,Math.max(.4,d)]);t+=d}return out}function wordCues(text){let w=text.split(/\s+/).filter(Boolean);if(w.length<2)return null;let wt=w.map(x=>x.replace(/[^\p{L}\p{N}]/gu,'').length+1),tot=wt.reduce((a,b)=>a+b,0),acc=0;return wt.map(v=>{let f=acc/tot;acc+=v;return Math.round(f*1000)/1000})}function speechWindows(segs,maxLen=28){let out=[];for(let [a,b] of segs){for(let t=a;t<b-.05;t+=maxLen)out.push([t,Math.min(b,t+maxLen)])}return out}function langLabel(c){try{return new Intl.DisplayNames(['en'],{type:'language'}).of(c)||c}catch{return c}}async function runAutoCaptions(){if(autoCapBusy)return;let c=getSelected(),st=$('#autoCapStatus'),btn=$('#autoCaptions');if(!c||!['audio','video'].includes(c.asset?.type))return toast('Select a video or audio clip with speech');let buf=audioBuffers.get(c.assetId);if(!buf)return toast('This clip has no decoded audio yet');if(!window.ffWhisper)return toast('Speech engine unavailable');let capLang=$('#capLang')?.value||'en';ffWhisper.setPack?.(capLang==='en'?'en':'multi');if(!ffWhisper.ready&&!(await ffWhisper.cached())&&autoCapArmed!==ffWhisper.pack){autoCapArmed=ffWhisper.pack;btn.textContent=`Download speech model (${(ffWhisper.packBytes/1048576).toFixed(0)} MB)`;st.textContent='One-time download from this site. It is checked and kept on this device for next time.';return}autoCapBusy=true;btn.disabled=true;try{if(!ffWhisper.ready){st.textContent='Loading speech model…';await ffWhisper.load(p=>st.textContent=`Loading speech model ${Math.round(p*100)}%`)}st.textContent='Finding speech…';let probs=speechProbs.get(c.assetId)||await sileroProbabilities(buf,p=>st.textContent=`Finding speech ${Math.round(p*100)}%`);speechProbs.set(c.assetId,probs);let lo=c.in,hi=c.in+c.duration*(c.speed||1),segs=planSpeech(probs,.032,.5,.15,.6,.3,.4).map(([a,b])=>[Math.max(lo,a),Math.min(hi,b)]).filter(([a,b])=>b-a>.3),wins=speechWindows(segs);if(!wins.length){st.textContent='No speech found in this clip.';return}let pcm=await mono16k(buf),made=0,t0=performance.now(),capDetected=null;snapshot();for(let i=0;i<wins.length;i++){let [a,b]=wins[i];st.textContent=`Transcribing ${i+1} of ${wins.length}…`;let r=await ffWhisper.transcribe(pcm.subarray(Math.floor(a*16000),Math.ceil(b*16000)),{language:capLang});if(capLang==='auto'&&r.language){capLang=r.language;capDetected=r.language}if(!r.text||/^\[.*\]$|^\(.*\)$/.test(r.text))continue;let sp=c.speed||1,at=x=>c.start+(a+x-c.in)/sp,segs2=r.segments?.length?r.segments:[{start:0,end:b-a,text:r.text}];for(let sg of segs2){if(!sg.text||/^\[.*\]$|^\(.*\)$/.test(sg.text))continue;for(let [txt,s0,d] of captionChunks(sg.text,at(sg.start),at(Math.min(b-a,sg.end)))){let cc=addCaption(txt.slice(0,500),s0,d);cc.wordTimings=wordCues(cc.text);made++}}}selected=c.id;renderAll();queueSave();let secs=((performance.now()-t0)/1000).toFixed(1);st.textContent=made?`Added ${made} caption${made>1?'s':''} in ${secs} s${capDetected?` (detected ${langLabel(capDetected)})`:''}. Edit them like any caption.`:'Speech found, but nothing was recognised.';toast(made?`${made} captions added`:'No captions added')}catch(e){st.textContent=e.message||'Transcription failed';toast('Auto captions failed')}finally{autoCapBusy=false;autoCapArmed=false;btn.disabled=false;btn.textContent='Transcribe selected clip'}}let txCut=new Set(),txFor=null,txQuery='';const FILLER=/^(u+m+|u+h+|u+h+m+|e+r+m*|h+m+|m+h*m+|a+h+)$/;function pauseRanges(src,words,maxGap=.6,keepGap=.25){let pieces=sourcePieces(src),out=[],edges=[];let first=words[0],last=words[words.length-1];for(let q of pieces){let ws=words.filter(w=>(w.a+w.b)/2>=q.start&&(w.a+w.b)/2<q.start+q.duration);let pts=[[q.start,q.start]].concat(ws.map(w=>[w.a,w.b])).concat([[q.start+q.duration,q.start+q.duration]]);for(let i=0;i+1<pts.length;i++){let a=pts[i][1],b=pts[i+1][0],pr=speechProbs.get(q.assetId);if(b-a<=maxGap)continue;if(pr){let sp=q.speed||1,src0=t=>q.in+(t-q.start)*sp,f=.032,quiet=0,n=0;for(let t=a;t<b;t+=f){n++;if((pr[Math.round(src0(t)/f)]??0)<.35)quiet++}if(quiet/Math.max(1,n)<.8)continue}let pad=keepGap/2,ca=i===0?a:a+pad,cb=i+2===pts.length?b:b-pad;if(cb-ca>.08)out.push([ca,cb,q])}}return out}const speechProbs=new Map();function transcriptSource(){let c=getSelected();if(c&&['audio','video'].includes(c.asset?.type))return c;if(c&&c.asset?.type==='caption'){let mid=c.start+c.duration/2;for(let tr of state.tracks)for(let q of tr.clips)if(['audio','video'].includes(q.asset?.type)&&audioBuffers.has(q.assetId)&&q.start<=mid&&q.start+q.duration>mid)return q}return null}function sourcePieces(src){let tr=state.tracks.find(t=>t.clips.includes(src));return tr?tr.clips.filter(q=>q.assetId===src.assetId).sort((a,b)=>a.start-b.start):[src]}function transcriptWords(src){let pieces=sourcePieces(src),inAny=(a,b)=>pieces.some(q=>a<q.start+q.duration-.01&&b>q.start+.01),caps=state.tracks.filter(t=>t.name==='Captions').flatMap(t=>t.clips).filter(q=>inAny(q.start,q.start+q.duration)).sort((a,b)=>a.start-b.start),out=[];for(let cap of caps){let w=(cap.text||'').split(/\s+/).filter(Boolean),st=Array.isArray(cap.wordTimings)&&cap.wordTimings.length===w.length?cap.wordTimings:w.map((_,i)=>i/w.length);w.forEach((word,i)=>out.push({word,cap,i,a:cap.start+st[i]*cap.duration,b:cap.start+(i+1<w.length?st[i+1]:1)*cap.duration}))}return out}function renderTranscript(){let box=$('#transcript');if(!box)return;let src=transcriptSource();if(!src){box.innerHTML='';return}let key=src.assetId;if(txFor!==key){txCut=new Set();txFor=key}let words=transcriptWords(src);if(!words.length){box.innerHTML='';return}let n=[...txCut].filter(k=>k<words.length).length;let q=txQuery.trim().toLowerCase(),norm=w=>w.toLowerCase().replace(/[^\p{L}\p{N}']/gu,''),hit=k=>q&&norm(words[k].word).includes(norm(q)),fillers=words.map((w,k)=>k).filter(k=>FILLER.test(norm(words[k].word))),hits=q?words.map((w,k)=>k).filter(hit):[];box.innerHTML=`<div class="txHead"><b>Transcript</b><small>Tap words to remove them from the clip.</small></div><div class="txTools"><input id="txSearch" type="search" maxlength="60" placeholder="Find words" value="${esc(txQuery)}" aria-label="Find words in transcript"><button id="txPickHits"${hits.length?'':' disabled'}>${hits.length?`Select ${hits.length} match${hits.length>1?'es':''}`:'Select matches'}</button></div><div class="txTools"><button id="txFillers"${fillers.length?'':' disabled'}>${fillers.length?`Select ${fillers.length} filler${fillers.length>1?'s':''}`:'No fillers found'}</button><button id="txPauses">Tighten pauses</button></div><div class="txWords">${words.map((w,k)=>`<button class="tw${txCut.has(k)?' cut':''}${hit(k)?' hit':''}" data-k="${k}" aria-pressed="${txCut.has(k)}">${esc(w.word)}</button>`).join('')}</div><button id="txApply" class="primary" style="width:100%;margin-top:10px"${n?'':' disabled'}>${n?`Remove ${n} word${n>1?'s':''}`:'Select words to remove'}</button>`;box.querySelectorAll('.tw').forEach(b=>b.onclick=()=>{let k=+b.dataset.k;txCut.has(k)?txCut.delete(k):txCut.add(k);renderTranscript()});$('#txApply').onclick=()=>applyTranscriptCut();let si=$('#txSearch');si.oninput=()=>{txQuery=si.value;let pos=si.selectionStart;renderTranscript();let n2=$('#txSearch');n2.focus();n2.setSelectionRange(pos,pos)};$('#txPickHits').onclick=()=>{hits.forEach(k=>txCut.add(k));renderTranscript()};$('#txFillers').onclick=()=>{fillers.forEach(k=>txCut.add(k));renderTranscript()};$('#txPauses').onclick=()=>{let r=pauseRanges(src,words);if(!r.length)return toast('No pauses longer than 0.6 s');applyTranscriptCut(r)}}function snapEdge(q,t,dir){let pr=speechProbs.get(q.assetId);if(!pr)return t;let sp=q.speed||1,s=q.in+(t-q.start)*sp,f=.032,i0=Math.round(s/f),best=i0,bv=pr[i0]??1;for(let d=-5;d<=5;d++){let i=i0+d*(dir||1),v=pr[i];if(v!==undefined&&v<bv-.02){bv=v;best=i}}return q.start+(best*f-q.in)/sp}function applyTranscriptCut(extra=[]){let src=transcriptSource();if(!src)return;let tr0=state.tracks.find(t=>t.clips.includes(src));if(!tr0)return;if(tr0.locked)return toast('Unlock the track first');let words=transcriptWords(src),pieces=sourcePieces(src),pieceAt=t=>pieces.find(q=>t>=q.start-.001&&t<q.start+q.duration+.001),cut=[];words.forEach((w,k)=>{if(!txCut.has(k))return;let q=pieceAt((w.a+w.b)/2);if(!q)return;let a=Math.max(q.start,w.a),b=Math.min(q.start+q.duration,w.b);let prev=words[k-1],next=words[k+1];if(!prev||!txCut.has(k-1))a=Math.max(q.start,snapEdge(q,a,-1));if(!next||!txCut.has(k+1))b=Math.min(q.start+q.duration,snapEdge(q,b,1));if(b>a)cut.push([a,b,q])});let nWords=cut.length;cut.push(...extra);cut.sort((x,y)=>x[0]-y[0]);let merged=[];for(let r of cut){let l=merged[merged.length-1];if(l&&l[2]===r[2]&&r[0]-l[1]<.06)l[1]=Math.max(l[1],r[1]);else merged.push([...r])}if(!merged.length)return;let keepFor=q=>{let rs=merged.filter(r=>r[2]===q),keep=[],t=0;for(let [a,b] of rs){let la=a-q.start,lb=b-q.start;if(la-t>.04)keep.push([t,la]);t=lb}if(q.duration-t>.04)keep.push([t,q.duration]);return keep};if(pieces.every(q=>!keepFor(q).length))return toast('That would remove the whole clip');let capWords=new Map();words.forEach((w,k)=>{if(!capWords.has(w.cap))capWords.set(w.cap,[]);capWords.get(w.cap).push({...w,dead:txCut.has(k)})});snapshot();let removedTotal=0;for(let q of [...pieces].reverse()){let rs=merged.filter(r=>r[2]===q);if(!rs.length)continue;let keep=keepFor(q),rem=rs.reduce((s,[a,b])=>s+b-a,0),end=q.start+q.duration,twins=[];for(let tr of state.tracks)for(let x of tr.clips)if(x===q||(tr.name!=='Captions'&&x.assetId===q.assetId&&Math.abs(x.start-q.start)<.05&&Math.abs(x.duration-q.duration)<.05))twins.push([tr,x]);for(let [tr,x] of twins){tr.clips=tr.clips.filter(y=>y!==x);let cursor=x.start;for(let [a,b] of keep){let sp=x.speed||1;tr.clips.push({...x,id:uid(),start:cursor,in:x.in+a*sp,duration:b-a,keyframes:{...(x.keyframes||{})}});cursor+=b-a}}for(let tr of state.tracks){if(tr.name==='Captions')continue;for(let y of tr.clips)if(y.start>=end-.01)y.start-=rem}removedTotal+=rem}let shift=x=>{let d=0;for(let [a,b] of merged){if(x>=b)d+=b-a;else if(x>a)d+=x-a}return x-d},lastEnd=Math.max(...pieces.map(q=>q.start+q.duration)),dead=new Set();for(let [cap,ws] of capWords){let live=ws.filter(w=>!w.dead);if(!live.length){dead.add(cap);continue}let a=shift(live[0].a),b=shift(live[live.length-1].b),d=Math.max(.3,b-a);cap.text=live.map(w=>w.word).join(' ');cap.name=cap.text.slice(0,28);cap.start=a;cap.duration=d;cap.wordTimings=live.length>1?live.map((w,i)=>i?Math.min(.999,Math.max(0,(shift(w.a)-a)/d)):0).map((v,i,arr)=>i?Math.max(v,arr[i-1]+.001):0):null}for(let tr of state.tracks){if(tr.name!=='Captions')continue;tr.clips=tr.clips.filter(q=>!dead.has(q));for(let q of tr.clips)if(!capWords.has(q)&&q.start>=lastEnd-.01)q.start-=removedTotal}let first=state.tracks.flatMap(t=>t.clips).filter(q=>q.assetId===src.assetId).sort((a,b)=>a.start-b.start)[0];selected=first?.id||null;txCut=new Set();renderAll();toast(nWords?`Removed ${nWords} word${nWords>1?'s':''} (${removedTotal.toFixed(1)} s)`:`Tightened ${extra.length} pause${extra.length>1?'s':''} (${removedTotal.toFixed(1)} s)`)}$('#autoCaptions').onclick=runAutoCaptions;{let sel=$('#capLang');if(sel){try{let v=localStorage.getItem('ff-cap-lang');if(v&&[...sel.options].some(o=>o.value===v))sel.value=v}catch{}sel.onchange=()=>{try{localStorage.setItem('ff-cap-lang',sel.value)}catch{}autoCapArmed=false;$('#autoCaptions').textContent='Transcribe selected clip';$('#autoCapStatus').textContent=sel.value==='en'?'':'Other languages use a separate 39 MB model. English stays the fastest and most accurate.'}}}$('#addCaption').onclick=()=>addCaption();$('#importSrt').onclick=()=>$('#srtFile').click();$('#srtFile').onchange=async e=>{let f=e.target.files[0];if(!f)return;if(f.size>5000000)return toast('Caption file exceeds 5 MB');parseSRT(await f.text())};$('#exportSrt').onclick=exportSRTFile;$('#exportVtt').onclick=exportVTTFile;$('#captionText').oninput=e=>{let c=mutableSelected();if(c){c.text=e.target.value;c.name=e.target.value.slice(0,28)||'Caption';renderAll()}};$('#captionPos').onchange=e=>{let c=mutableSelected();if(c){c.captionPos=e.target.value;c.captionY=e.target.value==='top'?-35:e.target.value==='middle'?0:34;$('#captionY').value=c.captionY;$('#captionYV').value=c.captionY+'%';draw(current);queueSave()}};$('#captionSize').oninput=e=>{let c=mutableSelected();if(c){c.captionSize=+e.target.value;$('#captionSizeV').value=c.captionSize+'px';draw(current);queueSave()}};$('#captionColor').oninput=e=>{let c=mutableSelected();if(c){c.captionColor=e.target.value;draw(current);queueSave()}};$('#captionFont').onchange=e=>{let c=mutableSelected();if(c){c.captionFont=e.target.value;draw(current);queueSave()}};$('#captionWeight').onchange=e=>{let c=mutableSelected();if(c){c.captionWeight=+e.target.value;draw(current);queueSave()}};$('#captionAlign').onchange=e=>{let c=mutableSelected();if(c){c.captionAlign=e.target.value;draw(current);queueSave()}};$('#captionStroke').oninput=e=>{let c=mutableSelected();if(c){c.captionStroke=+e.target.value;$('#captionStrokeV').value=e.target.value+'px';draw(current);queueSave()}};for(const [id,key,suffix] of [['captionX','captionX','%'],['captionY','captionY','%'],['captionWidth','captionWidth','%'],['captionLeading','captionLeading','%']])$('#'+id).oninput=e=>{let c=mutableSelected();if(c){c[key]=+e.target.value;$('#'+id+'V').value=e.target.value+suffix;draw(current);queueSave()}};for(const [id,key] of [['captionFadeIn','captionFadeIn'],['captionFadeOut','captionFadeOut']])$('#'+id).oninput=e=>{let c=mutableSelected();if(c){c[key]=+e.target.value/100;$('#'+id+'V').value=c[key].toFixed(2)+'s';draw(current);queueSave()}};$('#captionEase').onchange=e=>{let c=mutableSelected();if(c){c.captionEase=e.target.value;draw(current);queueSave()}};$('#captionKaraoke').onchange=e=>{let c=mutableSelected();if(c){c.captionKaraoke=e.target.value;draw(current);queueSave()}};$('#captionHighlight').oninput=e=>{let c=mutableSelected();if(c){c.captionHighlight=e.target.value;draw(current);queueSave()}};$('#applyCaptionTimings').onclick=()=>{let c=mutableSelected();if(!c)return;let words=(c.text||'').trim().split(/\s+/).filter(Boolean),vals=$('#captionTimings').value.split(',').map(x=>+x.trim()/100).filter(Number.isFinite);if(!vals.length){c.wordTimings=null;toast('Using even word timing')}else if(vals.length!==words.length||vals.some((v,i)=>v<0||v>=1||(i&&v<=vals[i-1])))return toast(`Enter ${words.length} ascending cue points from 0 to 99.9`);else{c.wordTimings=vals;toast(`${vals.length} word cues set`)}draw(current);queueSave()};$('#captionBg').oninput=e=>{let c=mutableSelected();if(c){c.captionBg=+e.target.value/100;$('#captionBgV').value=e.target.value+'%';draw(current);queueSave()}};$('#applyCaptionStyle').onclick=()=>{let c=mutableSelected();if(!c||c.asset?.type!=='caption')return;if(state.tracks.some(tr=>tr.locked&&tr.clips.some(q=>q.asset?.type==='caption'))){toast('Unlock caption tracks to apply style');return;}for(const tr of state.tracks)for(const q of tr.clips)if(q.asset?.type==='caption')Object.assign(q,{captionSize:c.captionSize||42,captionColor:c.captionColor||'#ffffff',captionBg:c.captionBg??.75,captionPos:c.captionPos||'bottom',captionFont:c.captionFont||'system-ui',captionWeight:c.captionWeight||700,captionAlign:c.captionAlign||'center',captionStroke:c.captionStroke??5,captionX:c.captionX||0,captionY:c.captionY??34,captionWidth:c.captionWidth||80,captionLeading:c.captionLeading||120,captionFadeIn:c.captionFadeIn??.2,captionFadeOut:c.captionFadeOut??.2,captionEase:c.captionEase||'smooth',captionKaraoke:c.captionKaraoke||'off',captionHighlight:c.captionHighlight||'#ffca5c'});renderAll();queueSave();toast('Caption style applied to all')};$('#addTitle').onclick=addTitle;$('#editorialPreset').onclick=()=>applyTitlePreset('editorial');$('#impactPreset').onclick=()=>applyTitlePreset('impact');$('#titleText').oninput=e=>{let c=mutableSelected();if(c){c.text=e.target.value;draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#fontSize').oninput=e=>{let c=mutableSelected();if(c){c.fontSize=+e.target.value;$('#fontSizeV').value=c.fontSize+'px';draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#titleColor').oninput=e=>{let c=mutableSelected();if(c){c.color=e.target.value;draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#titleAlign').onchange=e=>{let c=mutableSelected();if(c){c.align=e.target.value;draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#strokeWidth').oninput=e=>{let c=mutableSelected();if(c){c.strokeWidth=+e.target.value;$('#strokeV').value=c.strokeWidth+'px';draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#bgOpacity').oninput=e=>{let c=mutableSelected();if(c){c.bgOpacity=+e.target.value/100;$('#bgV').value=e.target.value+'%';draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};document.querySelectorAll('.fxPreset').forEach(b=>b.onclick=()=>{let c=mutableSelected();if(!c)return;let p={clean:{exposure:.08,contrast:1.12,saturation:1.05,temperature:0},film:{exposure:.05,contrast:1.08,saturation:.9,temperature:.38},night:{exposure:-.2,contrast:1.18,saturation:.72,temperature:-.45},bleach:{exposure:.12,contrast:1.35,saturation:.28,temperature:.12}}[b.dataset.fx];Object.assign(c,p);renderAll();toast(b.textContent+' applied')});$('#inspectBtn').onclick=()=>innerWidth<=620?mobileView('inspect'):document.querySelector('.right').classList.toggle('drawer');$('#tangentMode').onclick=e=>{tangentsLinked=!tangentsLinked;e.currentTarget.classList.toggle('active',tangentsLinked);e.currentTarget.textContent=tangentsLinked?'⛓ Linked tangents':'⛓ Broken tangents'};$('#fitCurve').onclick=()=>{let c=getSelected(),p=$('#curveProp').value,ks=c?.keyframes?.[p]||[];if(!ks.length)return;let [lo,hi]=curveRange(p),mn=Math.min(...ks.map(k=>k.v)),mx=Math.max(...ks.map(k=>k.v)),base=hi-lo;curveFit=Math.max(.25,Math.min(4,(mx-mn||base*.25)/base*1.35));renderCurve()};$('#closeInspector').onclick=()=>innerWidth<=620?mobileView('edit'):document.querySelector('.right').classList.remove('drawer');$('#curveProp').onchange=()=>{selectedKey=null;selectedKeys=[];renderCurve()};$('#easeMode').onchange=e=>{let c=mutableSelected(),p=$('#curveProp').value,ks=c?.keyframes?.[p]||[],ids=selectedKeys.filter(x=>x.prop===p).map(x=>x.i);if(!ids.length&&selectedKey?.prop===p)ids=[selectedKey.i];ids.forEach(i=>ks[i].ease=e.target.value);renderAll();queueSave()};$('#prevKey').onclick=()=>jumpKey(-1);$('#nextKey').onclick=()=>jumpKey(1);$('#deleteKey').onclick=deleteSelectedKey;$('#vaultBtn').onclick=()=>{if(vaultMeta&&vaultKey){vaultKey=null;state.assets=[];state.tracks.forEach(t=>t.clips=[]);vids.clear();audioBuffers.clear();renderAll();updateVaultUI();toast('Project locked')}else openVault(!!vaultMeta)};$('#vaultCancel').onclick=()=>{if(!vaultMeta||vaultKey)$('#vaultModal').classList.remove('show')};$('#vaultSubmit').onclick=submitVault;$('#vaultPass').onkeydown=e=>{if(e.key==='Enter')submitVault()};$('#saveBtn').onclick=()=>saveProject(true);$('#importBtn').onclick=()=>$('#file').click();$('#file').onchange=e=>{[...e.target.files].forEach(addAsset);toast(`${e.target.files.length} file${e.target.files.length===1?'':'s'} imported`)};$('#drop').ondragover=e=>e.preventDefault();$('#drop').ondrop=e=>{e.preventDefault();[...e.dataTransfer.files].forEach(addAsset)};$('#demoBtn').onclick=loadDemo;function setRange(which){if(which==='in'){inPoint=current;if(outPoint!==null&&outPoint<=inPoint)outPoint=null;toast('In '+fmt(inPoint))}else{outPoint=current;if(inPoint!==null&&inPoint>=outPoint)inPoint=null;toast('Out '+fmt(outPoint))}$('#setIn').classList.toggle('active',inPoint!==null);$('#setOut').classList.toggle('active',outPoint!==null);renderTimeline();queueSave()}$('#setIn').onclick=()=>setRange('in');$('#setOut').onclick=()=>setRange('out');$('#loop').onclick=e=>{loopRange=!loopRange;e.currentTarget.classList.toggle('active',loopRange);toast(loopRange?'Range loop on':'Range loop off');queueSave()};function shuttle(dir){if(playing&&shuttleDir===dir)shuttleRate=Math.min(4,shuttleRate*2);else{shuttleDir=dir;shuttleRate=1}setPlaying(true);toast(`${dir<0?'Reverse':'Forward'} ${shuttleRate}×`)}$('#rev').onclick=()=>shuttle(-1);$('#fwd').onclick=()=>shuttle(1);$('#play').onclick=()=>{shuttleDir=1;shuttleRate=1;setPlaying(!playing)};$('#back').onclick=()=>{current=0;updateHead()};$('#next').onclick=()=>{current=duration();updateHead()};$('#split').onclick=split;$('#delete').onclick=remove;$('#ripple').onclick=()=>{let c=mutableSelected();if(!c)return;let tr=state.tracks.find(t=>t.clips.includes(c));snapshot();let end=c.start+c.duration,d=c.duration;tr.clips=tr.clips.filter(x=>x!==c);for(const x of tr.clips)if(x.start>=end-.01)x.start=Math.max(c.start,x.start-d);selected=null;renderAll();toast('Ripple gap closed')};$('#marker').onclick=()=>{snapshot();state.markers.push({id:uid(),t:current,label:'Marker '+(state.markers.length+1)});renderAll();toast('Marker added')};document.querySelectorAll('.keybtn').forEach(b=>b.onclick=()=>addKey(b.dataset.prop));$('#duplicate').onclick=()=>{let c=mutableSelected();if(!c)return;snapshot();let n={...c,id:uid(),start:c.start+c.duration+.1};state.tracks.find(t=>t.clips.includes(c)).clips.push(n);selected=n.id;renderAll()};$('#addTrack').onclick=()=>{snapshot();let n=state.tracks.filter(t=>t.type==='video').length+1;state.tracks.splice(state.tracks.length-1,0,{id:uid(),name:'Video '+n,type:'video',clips:[]});renderAll()};$('#snap').onclick=e=>{snap=!snap;e.currentTarget.classList.toggle('active',snap)};$('#zoom').oninput=e=>{pxPerSec=+e.target.value;renderTimeline()};$('#exportBtn').onclick=doExport;$('#cancelExport').onclick=()=>{exporting=false;$('#modal').classList.remove('show')};for(const id of ['opacity','volume','clipSpeed','scale','x','y','pan','exposure','contrast','saturation','temperature'])$('#'+id).oninput=e=>{let c=mutableSelected();if(!c)return;c[id==='clipSpeed'?'speed':id]=+e.target.value/100;updateOutputs();draw(current)};$('#loadLut').onclick=()=>$('#lutFile').click();$('#lutFile').onchange=async e=>{let f=e.target.files[0],c=mutableSelected();if(!f||!c)return;if(f.size>10000000)return toast('LUT file exceeds 10 MB');try{let lut=parseCube(await f.text());lut.title=(f.name||'Local LUT').slice(0,120);c.lut=lut;$('#lutV').value=lut.title;draw(current);queueSave();toast(`Loaded ${lut.size}³ LUT`)}catch(err){toast(err.message||'Invalid LUT')}};for(const id of ['filter','transition'])$('#'+id).onchange=e=>{let c=mutableSelected();if(c)c[id]=e.target.value;draw(current)};$('#undo').onclick=()=>{if(!undoStack.length)return;redoStack.push(projectSnapshot());restore(undoStack.pop())};$('#redo').onclick=()=>{if(!redoStack.length)return;undoStack.push(projectSnapshot());restore(redoStack.pop())};$('#scroll').onpointerdown=e=>{if(e.target.classList.contains('lane')){let r=e.target.getBoundingClientRect();current=Math.max(0,(e.clientX-r.left)/pxPerSec);updateHead()}};let captionDrag=false,captionResize=false;$('#captionResize').onpointerdown=e=>{let c=mutableSelected();if(c?.asset?.type==='caption'){captionResize=true;try{e.currentTarget.setPointerCapture(e.pointerId)}catch(_){}e.stopPropagation()}};$('#captionResize').onpointermove=e=>{if(!captionResize)return;let c=mutableSelected(),r=canvas.getBoundingClientRect(),cx=r.left+r.width*(.5+(c.captionX||0)/100),newW=Math.abs(e.clientX-cx)*2;c.captionWidth=Math.max(30,Math.min(95,newW/r.width*100));$('#captionWidth').value=c.captionWidth;$('#captionWidthV').value=Math.round(c.captionWidth)+'%';draw(current)};$('#captionResize').onpointerup=()=>{if(captionResize){captionResize=false;queueSave()}};canvas.onpointerdown=e=>{let c=mutableSelected();if(c?.asset?.type==='caption'&&activeAt(current).some(q=>q.c===c)){captionDrag=true;try{canvas.setPointerCapture(e.pointerId)}catch(_){}e.preventDefault()}};canvas.onpointermove=e=>{if(!captionDrag)return;let c=mutableSelected(),r=canvas.getBoundingClientRect();c.captionX=Math.max(-40,Math.min(40,(e.clientX-r.left)/r.width*100-50));c.captionY=Math.max(-40,Math.min(40,(e.clientY-r.top)/r.height*100-50));c.captionPos=c.captionY<-18?'top':c.captionY>18?'bottom':'middle';$('#captionX').value=c.captionX;$('#captionY').value=c.captionY;$('#captionXV').value=Math.round(c.captionX)+'%';$('#captionYV').value=Math.round(c.captionY)+'%';$('#captionPos').value=c.captionPos;draw(current)};canvas.onpointerup=()=>{if(captionDrag){captionDrag=false;queueSave()}};document.querySelectorAll('[data-trim-mode]').forEach(b=>b.onclick=()=>{trimMode=b.dataset.trimMode;document.querySelectorAll('[data-trim-mode]').forEach(x=>{let on=x===b;x.classList.toggle('on',on);x.setAttribute('aria-checked',on)})});document.querySelectorAll('[data-trim-step]').forEach(b=>b.onclick=()=>trimStep(+b.dataset.trimStep));window.onkeydown=e=>{let tag=e.target?.tagName;if(['INPUT','TEXTAREA','SELECT'].includes(tag))return;if(e.code==='Space'||e.key.toLowerCase()==='k'){e.preventDefault();setPlaying(false);shuttleRate=1}if(e.key.toLowerCase()==='j')shuttle(-1);if(e.key.toLowerCase()==='l')shuttle(1);if(e.key.toLowerCase()==='i')setRange('in');if(e.key.toLowerCase()==='o')setRange('out');if(e.key.toLowerCase()==='s'&&!e.ctrlKey)split();if(e.key.toLowerCase()==='m')$('#marker').click();if(e.key.toLowerCase()==='f')fitTimeline();if(e.key==='/'){e.preventDefault();mobileView('media');$('#mediaSearch').focus()}if(e.key===',')nudgeSelected(e.shiftKey?-10:-1);if(e.key==='.')nudgeSelected(e.shiftKey?10:1);if(e.code==='BracketLeft'||e.code==='BracketRight'){e.preventDefault();let d=(e.code==='BracketLeft'?-1:1)*(e.shiftKey?10:1);(e.ctrlKey||e.metaKey)?slideSelected(d):e.altKey?rollSelected(d):slipSelected(d)}if((e.key==='Delete'||e.key==='Backspace')&&selected)remove()};window.addEventListener('resize',renderTimeline);window.__frameforge={state,placeAsset,pauseRanges,transcriptWords,applyTranscriptCut,get txCut(){return txCut},wordCues,runAutoCaptions,captionChunks,speechWindows,slipSelected,rollSelected,slideSelected,select(id){selected=id;renderAll()},magneticEdit,snapCandidates,nudgeSelected,loadDemo,addAsset,mediaKind,addCaption,parseSRT,planSpeech,planRestartCuts,mergeRanges,voicePolishSpec,swivelTransform,parseCube,sampleCube,applyLutPixels,renderClipSource,sileroProbabilities,localTranscript,applyKeepPlan,exportSRTFile,exportVTTFile,saveProject,restoreProject,split,export:doExport,get selected(){return getSelected()},setTime(t){current=t;updateHead()},get lastExport(){return window.__lastExport||null},get range(){return {in:inPoint,out:outPoint,loop:loopRange}},get workspace(){return {mediaQuery,mediaFilter,pxPerSec,visibleAssets:document.querySelectorAll('.asset').length,projectFps,projectAspect,canvas:[canvas.width,canvas.height]}},get vault(){return {enabled:!!vaultMeta,unlocked:!!vaultKey,kdf:vaultMeta?.kdf,cipher:vaultMeta?.cipher,iterations:vaultMeta?.iterations}},get monitor(){return {active:!!monitorAC,tracks:monitorNodes.size,sources:monitorSources.length,nodes:[...monitorNodes.entries()].map(([id,n])=>({id,low:n.low.gain.value,high:n.high.gain.value,threshold:n.comp.threshold.value,ratio:n.comp.ratio.value,gain:n.gain.gain.value,pan:n.pan?.pan.value}))}}};const FF_CORE_SHA256='370365cecc538e1c4a57cc53ba4fe05ba65db46107de4dfa51cf906a1fa70710';let useGlLut='auto';const glLut=(()=>{let cv=null,gl=null,prog=null,src=null,cube=null,cubeFor=null,u={},dead=false;const VS=`#version 300 es
+function mobileView(mode){if(innerWidth>620)return;document.querySelector('.side:not(.right)').classList.toggle('mobileOpen',mode==='media');document.querySelector('.right').classList.toggle('drawer',mode==='inspect');document.querySelectorAll('.mobileDock button').forEach(b=>b.classList.toggle('active',b.dataset.mobileView===mode));if(mode==='preview')document.querySelector('.viewer').scrollIntoView({block:'start'});if(mode==='edit')document.querySelector('.timeline').scrollIntoView({block:'start'})}$('#projectSettingsBtn').onclick=()=>{$('#projectSettingsPanel').classList.add('show');$('#projectAspect').value=projectAspect;$('#projectFps').value=projectFps};$('#closeProjectSettings').onclick=()=>$('#projectSettingsPanel').classList.remove('show');$('#applyProjectSettings').onclick=()=>{projectAspect=$('#projectAspect').value;projectFps=+$('#projectFps').value;applyProjectFormat();$('#projectSettingsPanel').classList.remove('show')};document.querySelectorAll('[data-mobile-view]').forEach(b=>b.onclick=()=>mobileView(b.dataset.mobileView));let autoCutPlan=null;$('#autoCutBtn').onclick=()=>{$('#autoCutPanel').classList.add('show')};$('#closeAutoCut').onclick=()=>$('#autoCutPanel').classList.remove('show');$('#vadThreshold').oninput=e=>$('#vadThresholdV').value=(+e.target.value/100).toFixed(2);$('#vadPadding').oninput=e=>$('#vadPaddingV').value=e.target.value+' ms';$('#voicePreset').onclick=()=>{let c=mutableSelected();if(!c)return;let tr=state.tracks.find(t=>t.clips.includes(c));if(tr?.type!=='audio')return toast('Select a voice audio clip');let p=voicePolishSpec();tr.voicePolish=p;tr.lowEQ=0;tr.highEQ=3;tr.compressor=35;renderMixer();queueSave();toast('Voice polish chain set')};$('#analyzeAutoCut').onclick=analyzeAutoCut;$('#applyAutoCut').onclick=()=>applyKeepPlan(autoCutPlan);$('#mixerBtn').onclick=()=>{$('#mixerPanel').classList.add('show');renderMixer()};$('#closeMixer').onclick=()=>$('#mixerPanel').classList.remove('show');$('#master').onpointerdown=()=>snapshot();$('#master').oninput=e=>{state.masterGain=dbToGain(+e.target.value);updateLiveAll();$('#masterV').value=(+e.target.value).toFixed(1)+' dB';$('#masterMeter').style.width=Math.min(100,state.masterGain*62)+'%';queueSave()};$('#mediaSearch').oninput=e=>{mediaQuery=e.target.value;renderLibrary()};$('#mediaKindFilter').onchange=e=>{mediaFilter=e.target.value;renderLibrary()};$('#fitTimeline').onclick=fitTimeline;$('#mediaTab').onclick=()=>{$('#mediaPanel').hidden=false;$('#textPanel').hidden=true;$('#fxPanel').hidden=true;$('#mediaTab').classList.add('active');$('#textTab').classList.remove('active')};let captionWhole=false,replacementExcluded=new Set(),replaceShowAll=false,replaceHistory=[];try{let h=JSON.parse(localStorage.getItem('ff-replace-history')||'[]');replaceHistory=Array.isArray(h)?h.slice(0,5):[]}catch(e){localStorage.removeItem('ff-replace-history')}$('#clearReplaceHistory').onclick=()=>{replaceHistory=[];localStorage.setItem('ff-replace-history','[]');renderReplaceHistory();$('#clearReplaceHistory').style.display='none';toast('Replacement history cleared')};$('#captionSearch').oninput=()=>{replacementExcluded.clear();replaceShowAll=false;renderCaptionList()};$('#captionReplace').oninput=replacePreview;$('#replaceSelectAll').onclick=()=>{replacementExcluded.clear();replacePreview()};$('#replaceSelectNone').onclick=()=>{state.tracks.flatMap(t=>t.clips).filter(c=>c.asset?.type==='caption').forEach(c=>replacementExcluded.add(c.id));replacePreview()};$('#replaceInvert').onclick=()=>{document.querySelectorAll('[data-replace-cue]').forEach(x=>x.checked?replacementExcluded.add(x.dataset.replaceCue):replacementExcluded.delete(x.dataset.replaceCue));replacePreview()};$('#replaceShowAll').onclick=()=>{replaceShowAll=!replaceShowAll;replacePreview()};$('#captionWhole').onclick=e=>{captionWhole=!captionWhole;e.currentTarget.classList.toggle('active',captionWhole);e.currentTarget.textContent=captionWhole?'Word ✓':'Word';replacePreview()};$('#replaceCaption').onclick=()=>replaceCaptionText(false);$('#replaceCaptions').onclick=()=>replaceCaptionText(true);$('#textTab').onclick=()=>{$('#mediaPanel').hidden=true;$('#textPanel').hidden=false;$('#fxPanel').hidden=true;$('#textTab').classList.add('active');$('#mediaTab').classList.remove('active');$('#fxTab').classList.remove('active')};$('#fxTab').onclick=()=>{$('#mediaPanel').hidden=true;$('#textPanel').hidden=true;$('#fxPanel').hidden=false;$('#fxTab').classList.add('active');$('#mediaTab').classList.remove('active');$('#textTab').classList.remove('active');requestAnimationFrame(()=>updateScope(true))};let autoCapBusy=false,autoCapArmed=false;const capPack=()=>($('#capLang')?.value||'en')==='en'?'en':$('#capModel')?.value==='small'?'small':'multi',SMALL_NOTE=()=>'It needs about 1.5 GB of free memory while it runs.'+(navigator.deviceMemory&&navigator.deviceMemory<4?' This device reports less than 4 GB of memory, so the browser may close the tab. Standard is safer here.':''),capRemoveRefresh=async()=>{let b=$('#capModelRemove');if(!b||!window.ffWhisper?.setPack)return;let prev=ffWhisper.pack;ffWhisper.setPack('small');let c=await ffWhisper.cached();ffWhisper.setPack(prev);b.hidden=!c};const CJK=/[\u3040-\u30ff\u3400-\u9fff\uf900-\ufaff\uac00-\ud7af]/;function cjkGroups(text,maxChars=16){let parts=text.replace(/\s+/g,'').split(/(?<=[。！？、，,.!?])/).filter(Boolean),groups=[],cur='';for(let p of parts){while(p.length>maxChars){if(cur){groups.push(cur);cur=''}groups.push(p.slice(0,maxChars));p=p.slice(maxChars)}if((cur+p).length>maxChars&&cur){groups.push(cur);cur=''}cur+=p}if(cur)groups.push(cur);return groups.reduce((o,g)=>(/^[。！？、，,.!?]+$/.test(g)&&o.length?o[o.length-1]+=g:o.push(g),o),[])}function captionChunks(text,a,b,maxWords=7){let w=text.split(/\s+/).filter(Boolean);if(!w.length)return [];let groups=[];if(CJK.test(text)&&text.replace(/\s+/g,'').length>16&&w.length<4)groups=cjkGroups(text);else{let n=Math.ceil(w.length/maxWords),per=Math.ceil(w.length/n);for(let i=0;i<w.length;i+=per)groups.push(w.slice(i,i+per).join(' '))}let total=groups.reduce((n,g)=>n+g.length,0)||1,t=a,out=[];for(let g of groups){let d=(b-a)*g.length/total;out.push([g,t,Math.max(.4,d)]);t+=d}return out}function wordCues(text){let w=text.split(/\s+/).filter(Boolean);if(w.length<2)return null;let wt=w.map(x=>x.replace(/[^\p{L}\p{N}]/gu,'').length+1),tot=wt.reduce((a,b)=>a+b,0),acc=0;return wt.map(v=>{let f=acc/tot;acc+=v;return Math.round(f*1000)/1000})}function speechWindows(segs,maxLen=28){let out=[];for(let [a,b] of segs){for(let t=a;t<b-.05;t+=maxLen)out.push([t,Math.min(b,t+maxLen)])}return out}function langLabel(c){try{return new Intl.DisplayNames(['en'],{type:'language'}).of(c)||c}catch{return c}}async function runAutoCaptions(){if(autoCapBusy)return;let c=getSelected(),st=$('#autoCapStatus'),btn=$('#autoCaptions');if(!c||!['audio','video'].includes(c.asset?.type))return toast('Select a video or audio clip with speech');let buf=audioBuffers.get(c.assetId);if(!buf)return toast('This clip has no decoded audio yet');if(!window.ffWhisper)return toast('Speech engine unavailable');let capLang=$('#capLang')?.value||'en';ffWhisper.setPack?.(capPack());if(!ffWhisper.ready&&!(await ffWhisper.cached())&&autoCapArmed!==ffWhisper.pack){autoCapArmed=ffWhisper.pack;btn.textContent=`Download speech model (${(ffWhisper.packBytes/1048576).toFixed(0)} MB)`;st.textContent='One-time download from this site. It is checked and kept on this device for next time.'+(ffWhisper.pack==='small'?' '+SMALL_NOTE():'');return}autoCapBusy=true;btn.disabled=true;try{if(!ffWhisper.ready){st.textContent='Loading speech model…';await ffWhisper.load(p=>st.textContent=`Loading speech model ${Math.round(p*100)}%`)}st.textContent='Finding speech…';let probs=speechProbs.get(c.assetId)||await sileroProbabilities(buf,p=>st.textContent=`Finding speech ${Math.round(p*100)}%`);speechProbs.set(c.assetId,probs);let lo=c.in,hi=c.in+c.duration*(c.speed||1),segs=planSpeech(probs,.032,.5,.15,.6,.3,.4).map(([a,b])=>[Math.max(lo,a),Math.min(hi,b)]).filter(([a,b])=>b-a>.3),wins=speechWindows(segs);if(!wins.length){st.textContent='No speech found in this clip.';return}let pcm=await mono16k(buf),made=0,t0=performance.now(),capDetected=null;snapshot();for(let i=0;i<wins.length;i++){let [a,b]=wins[i];st.textContent=`Transcribing ${i+1} of ${wins.length}…`;let r=await ffWhisper.transcribe(pcm.subarray(Math.floor(a*16000),Math.ceil(b*16000)),{language:capLang,verbatim:capLang==='en'&&!!$('#capFillers')?.checked,words:capLang==='en'&&!!$('#capFillers')?.checked});if(capLang==='auto'&&r.language){capLang=r.language;capDetected=r.language}if(!r.text||/^\[.*\]$|^\(.*\)$/.test(r.text))continue;let sp=c.speed||1,at=x=>c.start+(a+x-c.in)/sp,segs2=r.segments?.length?r.segments:[{start:0,end:b-a,text:r.text}],nw=x=>x.toLowerCase().replace(/[^\p{L}\p{N}']/gu,''),aw=(r.words||[]).map(w=>({w:w.word,a:at(w.start)})),ai=0;for(let sg of segs2){if(!sg.text||/^\[.*\]$|^\(.*\)$/.test(sg.text))continue;for(let [txt,s0,d] of captionChunks(sg.text,at(sg.start),at(Math.min(b-a,sg.end)))){let cc=addCaption(txt.slice(0,500),s0,d),tw=cc.text.split(/\s+/).filter(Boolean),seg=aw.slice(ai,ai+tw.length);if(tw.length&&seg.length===tw.length&&nw(seg[0].w)===nw(tw[0])){ai+=tw.length;let wt=seg.map(x=>Math.max(0,Math.min(.98,(x.a-cc.start)/Math.max(.01,cc.duration))));for(let k=1;k<wt.length;k++)wt[k]=Math.max(wt[k],wt[k-1]);cc.wordTimings=wt;cc.wordAligned=true}else cc.wordTimings=wordCues(cc.text);made++}}}selected=c.id;renderAll();queueSave();let secs=((performance.now()-t0)/1000).toFixed(1);st.textContent=made?`Added ${made} caption${made>1?'s':''} in ${secs} s${capDetected?` (detected ${langLabel(capDetected)})`:''}. Edit them like any caption.`:'Speech found, but nothing was recognised.';toast(made?`${made} captions added`:'No captions added')}catch(e){st.textContent=e.message||'Transcription failed';toast('Auto captions failed')}finally{autoCapBusy=false;autoCapArmed=false;capRemoveRefresh();btn.disabled=false;btn.textContent='Transcribe selected clip'}}let txCut=new Set(),txFor=null,txQuery='';const FILLER=/^(u+m+|u+h+|u+h+m+|e+r+m*|h+m+|m+h*m+|a+h+)$/;function pauseRanges(src,words,maxGap=.6,keepGap=.25){let pieces=sourcePieces(src),out=[],edges=[];let first=words[0],last=words[words.length-1];for(let q of pieces){let ws=words.filter(w=>(w.a+w.b)/2>=q.start&&(w.a+w.b)/2<q.start+q.duration);let pts=[[q.start,q.start]].concat(ws.map(w=>[w.a,w.b])).concat([[q.start+q.duration,q.start+q.duration]]);for(let i=0;i+1<pts.length;i++){let a=pts[i][1],b=pts[i+1][0],pr=speechProbs.get(q.assetId);if(b-a<=maxGap)continue;if(pr){let sp=q.speed||1,src0=t=>q.in+(t-q.start)*sp,f=.032,quiet=0,n=0;for(let t=a;t<b;t+=f){n++;if((pr[Math.round(src0(t)/f)]??0)<.35)quiet++}if(quiet/Math.max(1,n)<.8)continue}let pad=keepGap/2,ca=i===0?a:a+pad,cb=i+2===pts.length?b:b-pad;if(cb-ca>.08)out.push([ca,cb,q])}}return out}const speechProbs=new Map();function transcriptSource(){let c=getSelected();if(c&&['audio','video'].includes(c.asset?.type))return c;if(c&&c.asset?.type==='caption'){let mid=c.start+c.duration/2;for(let tr of state.tracks)for(let q of tr.clips)if(['audio','video'].includes(q.asset?.type)&&audioBuffers.has(q.assetId)&&q.start<=mid&&q.start+q.duration>mid)return q}return null}function sourcePieces(src){let tr=state.tracks.find(t=>t.clips.includes(src));return tr?tr.clips.filter(q=>q.assetId===src.assetId).sort((a,b)=>a.start-b.start):[src]}function transcriptWords(src){let pieces=sourcePieces(src),inAny=(a,b)=>pieces.some(q=>a<q.start+q.duration-.01&&b>q.start+.01),caps=state.tracks.filter(t=>t.name==='Captions').flatMap(t=>t.clips).filter(q=>inAny(q.start,q.start+q.duration)).sort((a,b)=>a.start-b.start),out=[];for(let cap of caps){let w=(cap.text||'').split(/\s+/).filter(Boolean),st=Array.isArray(cap.wordTimings)&&cap.wordTimings.length===w.length?cap.wordTimings:w.map((_,i)=>i/w.length);w.forEach((word,i)=>out.push({word,cap,i,a:cap.start+st[i]*cap.duration,b:cap.start+(i+1<w.length?st[i+1]:1)*cap.duration}))}return out}function renderTranscript(){let box=$('#transcript');if(!box)return;let src=transcriptSource();if(!src){box.innerHTML='';return}let key=src.assetId;if(txFor!==key){txCut=new Set();txFor=key}let words=transcriptWords(src);if(!words.length){box.innerHTML='';return}let n=[...txCut].filter(k=>k<words.length).length;let q=txQuery.trim().toLowerCase(),norm=w=>w.toLowerCase().replace(/[^\p{L}\p{N}']/gu,''),hit=k=>q&&norm(words[k].word).includes(norm(q)),fillers=words.map((w,k)=>k).filter(k=>FILLER.test(norm(words[k].word))),hits=q?words.map((w,k)=>k).filter(hit):[];box.innerHTML=`<div class="txHead"><b>Transcript</b><small>Tap words to remove them from the clip.</small></div><div class="txTools"><input id="txSearch" type="search" maxlength="60" placeholder="Find words" value="${esc(txQuery)}" aria-label="Find words in transcript"><button id="txPickHits"${hits.length?'':' disabled'}>${hits.length?`Select ${hits.length} match${hits.length>1?'es':''}`:'Select matches'}</button></div><div class="txTools"><button id="txFillers"${fillers.length?'':' disabled'}>${fillers.length?`Select ${fillers.length} filler${fillers.length>1?'s':''}`:'No fillers found'}</button><button id="txPauses">Tighten pauses</button></div><div class="txWords">${words.map((w,k)=>`<button class="tw${txCut.has(k)?' cut':''}${hit(k)?' hit':''}" data-k="${k}" aria-pressed="${txCut.has(k)}">${esc(w.word)}</button>`).join('')}</div><button id="txApply" class="primary" style="width:100%;margin-top:10px"${n?'':' disabled'}>${n?`Remove ${n} word${n>1?'s':''}`:'Select words to remove'}</button>`;box.querySelectorAll('.tw').forEach(b=>b.onclick=()=>{let k=+b.dataset.k;txCut.has(k)?txCut.delete(k):txCut.add(k);renderTranscript()});$('#txApply').onclick=()=>applyTranscriptCut();let si=$('#txSearch');si.oninput=()=>{txQuery=si.value;let pos=si.selectionStart;renderTranscript();let n2=$('#txSearch');n2.focus();n2.setSelectionRange(pos,pos)};$('#txPickHits').onclick=()=>{hits.forEach(k=>txCut.add(k));renderTranscript()};$('#txFillers').onclick=()=>{fillers.forEach(k=>txCut.add(k));renderTranscript()};$('#txPauses').onclick=()=>{let r=pauseRanges(src,words);if(!r.length)return toast('No pauses longer than 0.6 s');applyTranscriptCut(r)}}function snapEdge(q,t,dir){let pr=speechProbs.get(q.assetId);if(!pr)return t;let sp=q.speed||1,s=q.in+(t-q.start)*sp,f=.032,i0=Math.round(s/f),best=i0,bv=pr[i0]??1;for(let d=-5;d<=5;d++){let i=i0+d*(dir||1),v=pr[i];if(v!==undefined&&v<bv-.02){bv=v;best=i}}return q.start+(best*f-q.in)/sp}function snapWindow(q,t,lo,hi){let pr=speechProbs.get(q.assetId);if(!pr)return t;let sp=q.speed||1,f=.032,s=q.in+(t-q.start)*sp,i0=Math.max(0,Math.round((s+lo)/f)),i1=Math.round((s+hi)/f),best=Math.round(s/f),bv=pr[best]??1;for(let i=i0;i<=i1;i++){let v=pr[i];if(v!==undefined&&v<bv-.02){bv=v;best=i}}return q.start+(best*f-q.in)/sp}function applyTranscriptCut(extra=[]){let src=transcriptSource();if(!src)return;let tr0=state.tracks.find(t=>t.clips.includes(src));if(!tr0)return;if(tr0.locked)return toast('Unlock the track first');let words=transcriptWords(src),pieces=sourcePieces(src),pieceAt=t=>pieces.find(q=>t>=q.start-.001&&t<q.start+q.duration+.001),cut=[];words.forEach((w,k)=>{if(!txCut.has(k))return;let q=pieceAt((w.a+w.b)/2);if(!q)return;let a=Math.max(q.start,w.a),b=Math.min(q.start+q.duration,w.b);let prev=words[k-1],next=words[k+1],fil=w.cap.wordAligned&&FILLER.test(w.word.toLowerCase().replace(/[^\p{L}\p{N}']/gu,''));if(!prev||!txCut.has(k-1))a=Math.max(q.start,fil?snapWindow(q,a,-.3,.05):snapEdge(q,a,-1));if(!next||!txCut.has(k+1))b=Math.min(q.start+q.duration,fil?snapWindow(q,b,-.05,.15):snapEdge(q,b,1));if(b>a)cut.push([a,b,q])});let nWords=cut.length;cut.push(...extra);cut.sort((x,y)=>x[0]-y[0]);let merged=[];for(let r of cut){let l=merged[merged.length-1];if(l&&l[2]===r[2]&&r[0]-l[1]<.06)l[1]=Math.max(l[1],r[1]);else merged.push([...r])}if(!merged.length)return;let keepFor=q=>{let rs=merged.filter(r=>r[2]===q),keep=[],t=0;for(let [a,b] of rs){let la=a-q.start,lb=b-q.start;if(la-t>.04)keep.push([t,la]);t=lb}if(q.duration-t>.04)keep.push([t,q.duration]);return keep};if(pieces.every(q=>!keepFor(q).length))return toast('That would remove the whole clip');let capWords=new Map();words.forEach((w,k)=>{if(!capWords.has(w.cap))capWords.set(w.cap,[]);capWords.get(w.cap).push({...w,dead:txCut.has(k)})});snapshot();let removedTotal=0;for(let q of [...pieces].reverse()){let rs=merged.filter(r=>r[2]===q);if(!rs.length)continue;let keep=keepFor(q),rem=rs.reduce((s,[a,b])=>s+b-a,0),end=q.start+q.duration,twins=[];for(let tr of state.tracks)for(let x of tr.clips)if(x===q||(tr.name!=='Captions'&&x.assetId===q.assetId&&Math.abs(x.start-q.start)<.05&&Math.abs(x.duration-q.duration)<.05))twins.push([tr,x]);for(let [tr,x] of twins){tr.clips=tr.clips.filter(y=>y!==x);let cursor=x.start;for(let [a,b] of keep){let sp=x.speed||1;tr.clips.push({...x,id:uid(),start:cursor,in:x.in+a*sp,duration:b-a,keyframes:{...(x.keyframes||{})}});cursor+=b-a}}for(let tr of state.tracks){if(tr.name==='Captions')continue;for(let y of tr.clips)if(y.start>=end-.01)y.start-=rem}removedTotal+=rem}let shift=x=>{let d=0;for(let [a,b] of merged){if(x>=b)d+=b-a;else if(x>a)d+=x-a}return x-d},lastEnd=Math.max(...pieces.map(q=>q.start+q.duration)),dead=new Set();for(let [cap,ws] of capWords){let live=ws.filter(w=>!w.dead);if(!live.length){dead.add(cap);continue}let a=shift(live[0].a),b=shift(live[live.length-1].b),d=Math.max(.3,b-a);cap.text=live.map(w=>w.word).join(' ');cap.name=cap.text.slice(0,28);cap.start=a;cap.duration=d;cap.wordTimings=live.length>1?live.map((w,i)=>i?Math.min(.999,Math.max(0,(shift(w.a)-a)/d)):0).map((v,i,arr)=>i?Math.max(v,arr[i-1]+.001):0):null}for(let tr of state.tracks){if(tr.name!=='Captions')continue;tr.clips=tr.clips.filter(q=>!dead.has(q));for(let q of tr.clips)if(!capWords.has(q)&&q.start>=lastEnd-.01)q.start-=removedTotal}let first=state.tracks.flatMap(t=>t.clips).filter(q=>q.assetId===src.assetId).sort((a,b)=>a.start-b.start)[0];selected=first?.id||null;txCut=new Set();renderAll();toast(nWords?`Removed ${nWords} word${nWords>1?'s':''} (${removedTotal.toFixed(1)} s)`:`Tightened ${extra.length} pause${extra.length>1?'s':''} (${removedTotal.toFixed(1)} s)`)}$('#autoCaptions').onclick=runAutoCaptions;{let sel=$('#capLang');if(sel){try{let v=localStorage.getItem('ff-cap-lang');if(v&&[...sel.options].some(o=>o.value===v))sel.value=v}catch{}let mdl=$('#capModel'),row=$('#capModelRow');try{let m=localStorage.getItem('ff-cap-model');if(mdl&&(m==='multi'||m==='small'))mdl.value=m}catch{}let fil=$('#capFillers');try{if(fil)fil.checked=localStorage.getItem('ff-cap-fillers')==='1'}catch{}if(fil)fil.onchange=()=>{try{localStorage.setItem('ff-cap-fillers',fil.checked?'1':'0')}catch{}};let capUi=()=>{row&&(row.hidden=sel.value==='en');$('#capFillRow')&&($('#capFillRow').hidden=sel.value!=='en');autoCapArmed=false;$('#autoCaptions').textContent='Transcribe selected clip';$('#autoCapStatus').textContent=sel.value==='en'?'':mdl?.value==='small'?'High accuracy is a one-time 238 MB download. Best for Hindi and other Indic languages; expect to fix some spellings. '+SMALL_NOTE():'Other languages use a separate 39 MB model. For Hindi and other Indic languages, High accuracy is much better.'};capUi();$('#autoCapStatus').textContent='';sel.onchange=()=>{try{localStorage.setItem('ff-cap-lang',sel.value)}catch{}capUi()};if(mdl)mdl.onchange=()=>{try{localStorage.setItem('ff-cap-model',mdl.value)}catch{}capUi()};let rm=$('#capModelRemove');if(rm)rm.onclick=async()=>{await ffWhisper.forget('small');rm.hidden=true;toast('High-accuracy model removed')};capRemoveRefresh()}}$('#addCaption').onclick=()=>addCaption();$('#importSrt').onclick=()=>$('#srtFile').click();$('#srtFile').onchange=async e=>{let f=e.target.files[0];if(!f)return;if(f.size>5000000)return toast('Caption file exceeds 5 MB');parseSRT(await f.text())};$('#exportSrt').onclick=exportSRTFile;$('#exportVtt').onclick=exportVTTFile;$('#captionText').oninput=e=>{let c=mutableSelected();if(c){c.text=e.target.value;c.name=e.target.value.slice(0,28)||'Caption';renderAll()}};$('#captionPos').onchange=e=>{let c=mutableSelected();if(c){c.captionPos=e.target.value;c.captionY=e.target.value==='top'?-35:e.target.value==='middle'?0:34;$('#captionY').value=c.captionY;$('#captionYV').value=c.captionY+'%';draw(current);queueSave()}};$('#captionSize').oninput=e=>{let c=mutableSelected();if(c){c.captionSize=+e.target.value;$('#captionSizeV').value=c.captionSize+'px';draw(current);queueSave()}};$('#captionColor').oninput=e=>{let c=mutableSelected();if(c){c.captionColor=e.target.value;draw(current);queueSave()}};$('#captionFont').onchange=e=>{let c=mutableSelected();if(c){c.captionFont=e.target.value;draw(current);queueSave()}};$('#captionWeight').onchange=e=>{let c=mutableSelected();if(c){c.captionWeight=+e.target.value;draw(current);queueSave()}};$('#captionAlign').onchange=e=>{let c=mutableSelected();if(c){c.captionAlign=e.target.value;draw(current);queueSave()}};$('#captionStroke').oninput=e=>{let c=mutableSelected();if(c){c.captionStroke=+e.target.value;$('#captionStrokeV').value=e.target.value+'px';draw(current);queueSave()}};for(const [id,key,suffix] of [['captionX','captionX','%'],['captionY','captionY','%'],['captionWidth','captionWidth','%'],['captionLeading','captionLeading','%']])$('#'+id).oninput=e=>{let c=mutableSelected();if(c){c[key]=+e.target.value;$('#'+id+'V').value=e.target.value+suffix;draw(current);queueSave()}};for(const [id,key] of [['captionFadeIn','captionFadeIn'],['captionFadeOut','captionFadeOut']])$('#'+id).oninput=e=>{let c=mutableSelected();if(c){c[key]=+e.target.value/100;$('#'+id+'V').value=c[key].toFixed(2)+'s';draw(current);queueSave()}};$('#captionEase').onchange=e=>{let c=mutableSelected();if(c){c.captionEase=e.target.value;draw(current);queueSave()}};$('#captionKaraoke').onchange=e=>{let c=mutableSelected();if(c){c.captionKaraoke=e.target.value;draw(current);queueSave()}};$('#captionHighlight').oninput=e=>{let c=mutableSelected();if(c){c.captionHighlight=e.target.value;draw(current);queueSave()}};$('#applyCaptionTimings').onclick=()=>{let c=mutableSelected();if(!c)return;let words=(c.text||'').trim().split(/\s+/).filter(Boolean),vals=$('#captionTimings').value.split(',').map(x=>+x.trim()/100).filter(Number.isFinite);if(!vals.length){c.wordTimings=null;toast('Using even word timing')}else if(vals.length!==words.length||vals.some((v,i)=>v<0||v>=1||(i&&v<=vals[i-1])))return toast(`Enter ${words.length} ascending cue points from 0 to 99.9`);else{c.wordTimings=vals;toast(`${vals.length} word cues set`)}draw(current);queueSave()};$('#captionBg').oninput=e=>{let c=mutableSelected();if(c){c.captionBg=+e.target.value/100;$('#captionBgV').value=e.target.value+'%';draw(current);queueSave()}};$('#applyCaptionStyle').onclick=()=>{let c=mutableSelected();if(!c||c.asset?.type!=='caption')return;if(state.tracks.some(tr=>tr.locked&&tr.clips.some(q=>q.asset?.type==='caption'))){toast('Unlock caption tracks to apply style');return;}for(const tr of state.tracks)for(const q of tr.clips)if(q.asset?.type==='caption')Object.assign(q,{captionSize:c.captionSize||42,captionColor:c.captionColor||'#ffffff',captionBg:c.captionBg??.75,captionPos:c.captionPos||'bottom',captionFont:c.captionFont||'system-ui',captionWeight:c.captionWeight||700,captionAlign:c.captionAlign||'center',captionStroke:c.captionStroke??5,captionX:c.captionX||0,captionY:c.captionY??34,captionWidth:c.captionWidth||80,captionLeading:c.captionLeading||120,captionFadeIn:c.captionFadeIn??.2,captionFadeOut:c.captionFadeOut??.2,captionEase:c.captionEase||'smooth',captionKaraoke:c.captionKaraoke||'off',captionHighlight:c.captionHighlight||'#ffca5c'});renderAll();queueSave();toast('Caption style applied to all')};$('#addTitle').onclick=addTitle;$('#editorialPreset').onclick=()=>applyTitlePreset('editorial');$('#impactPreset').onclick=()=>applyTitlePreset('impact');$('#titleText').oninput=e=>{let c=mutableSelected();if(c){c.text=e.target.value;draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#fontSize').oninput=e=>{let c=mutableSelected();if(c){c.fontSize=+e.target.value;$('#fontSizeV').value=c.fontSize+'px';draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#titleColor').oninput=e=>{let c=mutableSelected();if(c){c.color=e.target.value;draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#titleAlign').onchange=e=>{let c=mutableSelected();if(c){c.align=e.target.value;draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#strokeWidth').oninput=e=>{let c=mutableSelected();if(c){c.strokeWidth=+e.target.value;$('#strokeV').value=c.strokeWidth+'px';draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};$('#bgOpacity').oninput=e=>{let c=mutableSelected();if(c){c.bgOpacity=+e.target.value/100;$('#bgV').value=e.target.value+'%';draw(current);if($('#mixerPanel').classList.contains('show'))renderMixer();queueSave()}};document.querySelectorAll('.fxPreset').forEach(b=>b.onclick=()=>{let c=mutableSelected();if(!c)return;let p={clean:{exposure:.08,contrast:1.12,saturation:1.05,temperature:0},film:{exposure:.05,contrast:1.08,saturation:.9,temperature:.38},night:{exposure:-.2,contrast:1.18,saturation:.72,temperature:-.45},bleach:{exposure:.12,contrast:1.35,saturation:.28,temperature:.12}}[b.dataset.fx];Object.assign(c,p);renderAll();toast(b.textContent+' applied')});$('#inspectBtn').onclick=()=>innerWidth<=620?mobileView('inspect'):document.querySelector('.right').classList.toggle('drawer');$('#tangentMode').onclick=e=>{tangentsLinked=!tangentsLinked;e.currentTarget.classList.toggle('active',tangentsLinked);e.currentTarget.textContent=tangentsLinked?'⛓ Linked tangents':'⛓ Broken tangents'};$('#fitCurve').onclick=()=>{let c=getSelected(),p=$('#curveProp').value,ks=c?.keyframes?.[p]||[];if(!ks.length)return;let [lo,hi]=curveRange(p),mn=Math.min(...ks.map(k=>k.v)),mx=Math.max(...ks.map(k=>k.v)),base=hi-lo;curveFit=Math.max(.25,Math.min(4,(mx-mn||base*.25)/base*1.35));renderCurve()};$('#closeInspector').onclick=()=>innerWidth<=620?mobileView('edit'):document.querySelector('.right').classList.remove('drawer');$('#curveProp').onchange=()=>{selectedKey=null;selectedKeys=[];renderCurve()};$('#easeMode').onchange=e=>{let c=mutableSelected(),p=$('#curveProp').value,ks=c?.keyframes?.[p]||[],ids=selectedKeys.filter(x=>x.prop===p).map(x=>x.i);if(!ids.length&&selectedKey?.prop===p)ids=[selectedKey.i];ids.forEach(i=>ks[i].ease=e.target.value);renderAll();queueSave()};$('#prevKey').onclick=()=>jumpKey(-1);$('#nextKey').onclick=()=>jumpKey(1);$('#deleteKey').onclick=deleteSelectedKey;$('#vaultBtn').onclick=()=>{if(vaultMeta&&vaultKey){vaultKey=null;state.assets=[];state.tracks.forEach(t=>t.clips=[]);vids.clear();audioBuffers.clear();renderAll();updateVaultUI();toast('Project locked')}else openVault(!!vaultMeta)};$('#vaultCancel').onclick=()=>{if(!vaultMeta||vaultKey)$('#vaultModal').classList.remove('show')};$('#vaultSubmit').onclick=submitVault;$('#vaultPass').onkeydown=e=>{if(e.key==='Enter')submitVault()};$('#saveBtn').onclick=()=>saveProject(true);$('#importBtn').onclick=()=>$('#file').click();$('#file').onchange=e=>{[...e.target.files].forEach(addAsset);toast(`${e.target.files.length} file${e.target.files.length===1?'':'s'} imported`)};$('#drop').ondragover=e=>e.preventDefault();$('#drop').ondrop=e=>{e.preventDefault();[...e.dataTransfer.files].forEach(addAsset)};$('#demoBtn').onclick=loadDemo;function setRange(which){if(which==='in'){inPoint=current;if(outPoint!==null&&outPoint<=inPoint)outPoint=null;toast('In '+fmt(inPoint))}else{outPoint=current;if(inPoint!==null&&inPoint>=outPoint)inPoint=null;toast('Out '+fmt(outPoint))}$('#setIn').classList.toggle('active',inPoint!==null);$('#setOut').classList.toggle('active',outPoint!==null);renderTimeline();queueSave()}$('#setIn').onclick=()=>setRange('in');$('#setOut').onclick=()=>setRange('out');$('#loop').onclick=e=>{loopRange=!loopRange;e.currentTarget.classList.toggle('active',loopRange);toast(loopRange?'Range loop on':'Range loop off');queueSave()};function shuttle(dir){if(playing&&shuttleDir===dir)shuttleRate=Math.min(4,shuttleRate*2);else{shuttleDir=dir;shuttleRate=1}setPlaying(true);toast(`${dir<0?'Reverse':'Forward'} ${shuttleRate}×`)}$('#rev').onclick=()=>shuttle(-1);$('#fwd').onclick=()=>shuttle(1);$('#play').onclick=()=>{shuttleDir=1;shuttleRate=1;setPlaying(!playing)};$('#back').onclick=()=>{current=0;updateHead()};$('#next').onclick=()=>{current=duration();updateHead()};$('#split').onclick=split;$('#delete').onclick=remove;$('#ripple').onclick=()=>{let c=mutableSelected();if(!c)return;let tr=state.tracks.find(t=>t.clips.includes(c));snapshot();let end=c.start+c.duration,d=c.duration;tr.clips=tr.clips.filter(x=>x!==c);for(const x of tr.clips)if(x.start>=end-.01)x.start=Math.max(c.start,x.start-d);selected=null;renderAll();toast('Ripple gap closed')};$('#marker').onclick=()=>{snapshot();state.markers.push({id:uid(),t:current,label:'Marker '+(state.markers.length+1)});renderAll();toast('Marker added')};document.querySelectorAll('.keybtn').forEach(b=>b.onclick=()=>addKey(b.dataset.prop));$('#duplicate').onclick=()=>{let c=mutableSelected();if(!c)return;snapshot();let n={...c,id:uid(),start:c.start+c.duration+.1};state.tracks.find(t=>t.clips.includes(c)).clips.push(n);selected=n.id;renderAll()};$('#addTrack').onclick=()=>{snapshot();let n=state.tracks.filter(t=>t.type==='video').length+1;state.tracks.splice(state.tracks.length-1,0,{id:uid(),name:'Video '+n,type:'video',clips:[]});renderAll()};$('#snap').onclick=e=>{snap=!snap;e.currentTarget.classList.toggle('active',snap)};$('#zoom').oninput=e=>{pxPerSec=+e.target.value;renderTimeline()};$('#exportBtn').onclick=doExport;$('#cancelExport').onclick=()=>{exporting=false;$('#modal').classList.remove('show')};for(const id of ['opacity','volume','clipSpeed','scale','x','y','pan','exposure','contrast','saturation','temperature'])$('#'+id).oninput=e=>{let c=mutableSelected();if(!c)return;c[id==='clipSpeed'?'speed':id]=+e.target.value/100;updateOutputs();draw(current)};$('#loadLut').onclick=()=>$('#lutFile').click();$('#lutFile').onchange=async e=>{let f=e.target.files[0],c=mutableSelected();if(!f||!c)return;if(f.size>10000000)return toast('LUT file exceeds 10 MB');try{let lut=parseCube(await f.text());lut.title=(f.name||'Local LUT').slice(0,120);c.lut=lut;$('#lutV').value=lut.title;draw(current);queueSave();toast(`Loaded ${lut.size}³ LUT`)}catch(err){toast(err.message||'Invalid LUT')}};for(const id of ['filter','transition'])$('#'+id).onchange=e=>{let c=mutableSelected();if(c)c[id]=e.target.value;draw(current)};$('#undo').onclick=()=>{if(!undoStack.length)return;redoStack.push(projectSnapshot());restore(undoStack.pop())};$('#redo').onclick=()=>{if(!redoStack.length)return;undoStack.push(projectSnapshot());restore(redoStack.pop())};$('#scroll').onpointerdown=e=>{if(e.target.classList.contains('lane')){let r=e.target.getBoundingClientRect();current=Math.max(0,(e.clientX-r.left)/pxPerSec);updateHead()}};let captionDrag=false,captionResize=false;$('#captionResize').onpointerdown=e=>{let c=mutableSelected();if(c?.asset?.type==='caption'){captionResize=true;try{e.currentTarget.setPointerCapture(e.pointerId)}catch(_){}e.stopPropagation()}};$('#captionResize').onpointermove=e=>{if(!captionResize)return;let c=mutableSelected(),r=canvas.getBoundingClientRect(),cx=r.left+r.width*(.5+(c.captionX||0)/100),newW=Math.abs(e.clientX-cx)*2;c.captionWidth=Math.max(30,Math.min(95,newW/r.width*100));$('#captionWidth').value=c.captionWidth;$('#captionWidthV').value=Math.round(c.captionWidth)+'%';draw(current)};$('#captionResize').onpointerup=()=>{if(captionResize){captionResize=false;queueSave()}};canvas.onpointerdown=e=>{let c=mutableSelected();if(c?.asset?.type==='caption'&&activeAt(current).some(q=>q.c===c)){captionDrag=true;try{canvas.setPointerCapture(e.pointerId)}catch(_){}e.preventDefault()}};canvas.onpointermove=e=>{if(!captionDrag)return;let c=mutableSelected(),r=canvas.getBoundingClientRect();c.captionX=Math.max(-40,Math.min(40,(e.clientX-r.left)/r.width*100-50));c.captionY=Math.max(-40,Math.min(40,(e.clientY-r.top)/r.height*100-50));c.captionPos=c.captionY<-18?'top':c.captionY>18?'bottom':'middle';$('#captionX').value=c.captionX;$('#captionY').value=c.captionY;$('#captionXV').value=Math.round(c.captionX)+'%';$('#captionYV').value=Math.round(c.captionY)+'%';$('#captionPos').value=c.captionPos;draw(current)};canvas.onpointerup=()=>{if(captionDrag){captionDrag=false;queueSave()}};document.querySelectorAll('[data-trim-mode]').forEach(b=>b.onclick=()=>{trimMode=b.dataset.trimMode;document.querySelectorAll('[data-trim-mode]').forEach(x=>{let on=x===b;x.classList.toggle('on',on);x.setAttribute('aria-checked',on)})});document.querySelectorAll('[data-trim-step]').forEach(b=>b.onclick=()=>trimStep(+b.dataset.trimStep));window.onkeydown=e=>{let tag=e.target?.tagName;if(['INPUT','TEXTAREA','SELECT'].includes(tag))return;if(e.code==='Space'||e.key.toLowerCase()==='k'){e.preventDefault();setPlaying(false);shuttleRate=1}if(e.key.toLowerCase()==='j')shuttle(-1);if(e.key.toLowerCase()==='l')shuttle(1);if(e.key.toLowerCase()==='i')setRange('in');if(e.key.toLowerCase()==='o')setRange('out');if(e.key.toLowerCase()==='s'&&!e.ctrlKey)split();if(e.key.toLowerCase()==='m')$('#marker').click();if(e.key.toLowerCase()==='f')fitTimeline();if(e.key==='/'){e.preventDefault();mobileView('media');$('#mediaSearch').focus()}if(e.key===',')nudgeSelected(e.shiftKey?-10:-1);if(e.key==='.')nudgeSelected(e.shiftKey?10:1);if(e.code==='BracketLeft'||e.code==='BracketRight'){e.preventDefault();let d=(e.code==='BracketLeft'?-1:1)*(e.shiftKey?10:1);(e.ctrlKey||e.metaKey)?slideSelected(d):e.altKey?rollSelected(d):slipSelected(d)}if((e.key==='Delete'||e.key==='Backspace')&&selected)remove()};window.addEventListener('resize',renderTimeline);window.__frameforge={state,placeAsset,pauseRanges,transcriptWords,applyTranscriptCut,get txCut(){return txCut},wordCues,runAutoCaptions,captionChunks,speechWindows,slipSelected,rollSelected,slideSelected,select(id){selected=id;renderAll()},magneticEdit,snapCandidates,nudgeSelected,loadDemo,addAsset,mediaKind,addCaption,parseSRT,planSpeech,planRestartCuts,mergeRanges,voicePolishSpec,swivelTransform,parseCube,sampleCube,applyLutPixels,renderClipSource,sileroProbabilities,localTranscript,applyKeepPlan,exportSRTFile,exportVTTFile,saveProject,restoreProject,split,export:doExport,get selected(){return getSelected()},setTime(t){current=t;updateHead()},get lastExport(){return window.__lastExport||null},get range(){return {in:inPoint,out:outPoint,loop:loopRange}},get workspace(){return {mediaQuery,mediaFilter,pxPerSec,visibleAssets:document.querySelectorAll('.asset').length,projectFps,projectAspect,canvas:[canvas.width,canvas.height]}},get vault(){return {enabled:!!vaultMeta,unlocked:!!vaultKey,kdf:vaultMeta?.kdf,cipher:vaultMeta?.cipher,iterations:vaultMeta?.iterations}},get monitor(){return {active:!!monitorAC,tracks:monitorNodes.size,sources:monitorSources.length,nodes:[...monitorNodes.entries()].map(([id,n])=>({id,low:n.low.gain.value,high:n.high.gain.value,threshold:n.comp.threshold.value,ratio:n.comp.ratio.value,gain:n.gain.gain.value,pan:n.pan?.pan.value}))}}};const FF_CORE_SHA256='370365cecc538e1c4a57cc53ba4fe05ba65db46107de4dfa51cf906a1fa70710';let useGlLut='auto';const glLut=(()=>{let cv=null,gl=null,prog=null,src=null,cube=null,cubeFor=null,u={},dead=false;const VS=`#version 300 es
 in vec2 p;out vec2 uv;void main(){uv=vec2(p.x*.5+.5,.5-p.y*.5);gl_Position=vec4(p,0.,1.);}`,FS=`#version 300 es
 precision highp float;precision highp sampler3D;uniform sampler2D s;uniform sampler3D l;uniform vec3 dmin,dmax;uniform float n;in vec2 uv;out vec4 o;void main(){vec4 c=texture(s,uv);vec3 x=clamp((c.rgb-dmin)/max(dmax-dmin,vec3(1e-6)),0.,1.);o=vec4(clamp(texture(l,(x*(n-1.)+.5)/n).rgb,0.,1.),c.a);}`;function sh(t,x){let o=gl.createShader(t);gl.shaderSource(o,x);gl.compileShader(o);if(!gl.getShaderParameter(o,gl.COMPILE_STATUS))throw Error('shader');return o}function init(){if(gl||dead)return !!gl;try{cv=document.createElement('canvas');gl=cv.getContext('webgl2',{premultipliedAlpha:false,antialias:false,depth:false,stencil:false,preserveDrawingBuffer:true,powerPreference:'high-performance'});if(!gl)throw Error('no webgl2');prog=gl.createProgram();gl.attachShader(prog,sh(gl.VERTEX_SHADER,VS));gl.attachShader(prog,sh(gl.FRAGMENT_SHADER,FS));gl.linkProgram(prog);if(!gl.getProgramParameter(prog,gl.LINK_STATUS))throw Error('link');gl.useProgram(prog);for(const k of['s','l','dmin','dmax','n'])u[k]=gl.getUniformLocation(prog,k);gl.uniform1i(u.s,0);gl.uniform1i(u.l,1);let b=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,1,-1,-1,1,1,1]),gl.STATIC_DRAW);let loc=gl.getAttribLocation(prog,'p');gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,2,gl.FLOAT,false,0,0);src=gl.createTexture();gl.activeTexture(gl.TEXTURE0);gl.bindTexture(gl.TEXTURE_2D,src);for(const[k,v]of[[gl.TEXTURE_MIN_FILTER,gl.NEAREST],[gl.TEXTURE_MAG_FILTER,gl.NEAREST],[gl.TEXTURE_WRAP_S,gl.CLAMP_TO_EDGE],[gl.TEXTURE_WRAP_T,gl.CLAMP_TO_EDGE]])gl.texParameteri(gl.TEXTURE_2D,k,v);cube=gl.createTexture();gl.activeTexture(gl.TEXTURE1);gl.bindTexture(gl.TEXTURE_3D,cube);for(const[k,v]of[[gl.TEXTURE_MIN_FILTER,gl.LINEAR],[gl.TEXTURE_MAG_FILTER,gl.LINEAR],[gl.TEXTURE_WRAP_S,gl.CLAMP_TO_EDGE],[gl.TEXTURE_WRAP_T,gl.CLAMP_TO_EDGE],[gl.TEXTURE_WRAP_R,gl.CLAMP_TO_EDGE]])gl.texParameteri(gl.TEXTURE_3D,k,v);gl.pixelStorei(gl.UNPACK_ALIGNMENT,1);gl.pixelStorei(gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL,false);gl.pixelStorei(gl.UNPACK_COLORSPACE_CONVERSION_WEBGL,gl.NONE);cv.addEventListener('webglcontextlost',e=>{e.preventDefault();gl=null;cubeFor=null;dead=true;updateEngineUI()});return true}catch(e){gl=null;dead=true;return false}}function apply(source,lut){if(!init())return null;try{let n=lut.size;if(cubeFor!==lut){let f=new Float32Array(n*n*n*3);for(let i=0;i<lut.data.length;i++){let r=lut.data[i];f[i*3]=r[0];f[i*3+1]=r[1];f[i*3+2]=r[2]}gl.activeTexture(gl.TEXTURE1);gl.texImage3D(gl.TEXTURE_3D,0,gl.RGB16F,n,n,n,0,gl.RGB,gl.FLOAT,f);cubeFor=lut}let w=source.width,h=source.height;if(cv.width!==w||cv.height!==h){cv.width=w;cv.height=h}gl.viewport(0,0,w,h);gl.activeTexture(gl.TEXTURE0);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,gl.RGBA,gl.UNSIGNED_BYTE,source);gl.uniform3fv(u.dmin,lut.domainMin);gl.uniform3fv(u.dmax,lut.domainMax);gl.uniform1f(u.n,n);gl.drawArrays(gl.TRIANGLE_STRIP,0,4);return cv}catch(e){dead=true;gl=null;updateEngineUI();return null}}return{ready:()=>init(),apply,get renderer(){if(!init())return '';let e=gl.getExtension('WEBGL_debug_renderer_info');return e?String(gl.getParameter(e.UNMASKED_RENDERER_WEBGL)).slice(0,120):'webgl2'},get fast(){return !/swiftshader|llvmpipe|software/i.test(this.renderer)},get active(){return !!gl&&!dead},read(w,h){let o=new Uint8Array(w*h*4);gl.readPixels(0,0,w,h,gl.RGBA,gl.UNSIGNED_BYTE,o);return o}}})();window.__ffLutPath=()=>(lutPath(),lutPath.bench||{pick:lutPath()});window.__ffGlParity=()=>{let n=33,t=`LUT_3D_SIZE ${n}\n`;for(let b=0;b<n;b++)for(let g=0;g<n;g++)for(let r=0;r<n;r++)t+=`${Math.min(1,Math.pow(r/32,.8)*1.05)} ${g/32*.9+.03} ${Math.pow(b/32,1.2)}\n`;let lut=parseCube(t),W=256,H=64,c=document.createElement('canvas');c.width=W;c.height=H;let x=c.getContext('2d',{willReadFrequently:true}),img=x.createImageData(W,H);for(let y=0;y<H;y++)for(let i=0;i<W;i++){let k=(y*W+i)*4;img.data[k]=i;img.data[k+1]=(y*4+i*3)&255;img.data[k+2]=(255-i+y*7)&255;img.data[k+3]=255}x.putImageData(img,0,0);let ref=applyLutPixels(x.getImageData(0,0,W,H),lut).data;if(!glLut.apply(c,lut))return {ok:false,note:'WebGL2 unavailable'};let g=glLut.read(W,H),max=0,sum=0;for(let y=0;y<H;y++)for(let i=0;i<W;i++)for(let ch=0;ch<3;ch++){let d=Math.abs(g[((H-1-y)*W+i)*4+ch]-ref[(y*W+i)*4+ch]);sum+=d;if(d>max)max=d}return {ok:max<=3,maxDiff:max,meanDiff:+(sum/(W*H*3)).toFixed(3),reference:ffCore?'zig':'js'}};function lutPath(){if(useGlLut===false||!glLut.ready())return 'cpu';if(useGlLut===true)return 'gl';if(lutPath.pick)return lutPath.pick;if(!glLut.fast){lutPath.pick='cpu';lutPath.bench={renderer:glLut.renderer,pick:'cpu',note:'software WebGL'};return 'cpu'}let W=960,H=540,a=document.createElement('canvas');a.width=W;a.height=H;let ax=a.getContext('2d');ax.fillStyle='#456';ax.fillRect(0,0,W,H);let o=document.createElement('canvas');o.width=W;o.height=H;let ox=o.getContext('2d'),n=9,t=`LUT_3D_SIZE ${n}\n`;for(let i=0;i<n**3;i++)t+='0.5 0.5 0.5\n';let lut=parseCube(t),time=f=>{f();let t0=performance.now();for(let i=0;i<3;i++)f();return (performance.now()-t0)/3},g=time(()=>{ox.drawImage(glLut.apply(a,lut),0,0);ox.getImageData(0,0,1,1)}),c=time(()=>{let b=document.createElement('canvas');b.width=W;b.height=H;let bx=b.getContext('2d',{willReadFrequently:true});bx.drawImage(a,0,0);let p=bx.getImageData(0,0,W,H);bx.putImageData(applyLutPixels(p,lut),0,0);ox.drawImage(b,0,0);ox.getImageData(0,0,1,1)});lutPath.pick=g<c*.8?'gl':'cpu';lutPath.bench={glMs:+g.toFixed(1),cpuMs:+c.toFixed(1),renderer:glLut.renderer,pick:lutPath.pick};updateEngineUI();return lutPath.pick}function updateEngineUI(){let r=$('#engineReadout'),d=$('#engineDetail'),row=$('#engineRow');if(!r)return;row.classList.toggle('on',!!ffCore);if(ffCore){r.textContent='Zig core active';d.textContent=(lutPath.pick==='gl'?'Preview grade on WebGL2 · ':'')+'Verified WebAssembly · export grade and waveforms'}else{r.textContent='JavaScript renderer';d.textContent=ffCoreState.note}}async function loadFFCore(){let t0=performance.now();try{if(!window.WebAssembly||!window.createFFCore||!crypto.subtle)throw Error('WebAssembly unavailable');let res=await fetch('core/frameforge-core.wasm',{credentials:'omit',cache:'no-cache'});if(!res.ok)throw Error('Core missing');let bytes=await res.arrayBuffer();if(bytes.byteLength>1048576)throw Error('Core too large');let hex=[...new Uint8Array(await crypto.subtle.digest('SHA-256',bytes))].map(b=>b.toString(16).padStart(2,'0')).join('');if(hex!==FF_CORE_SHA256)throw Error('Core integrity check failed');ffCore=createFFCore(await WebAssembly.compile(bytes));if(window.ffWhisper)ffWhisper.useCore(ffCore);ffCoreState={kind:'zig',ms:Math.round(performance.now()-t0)}}catch(e){ffCore=null;ffCoreState={kind:'js',note:(e.message||'Core unavailable')+' · JavaScript fallback active'}}updateEngineUI();window.__ffCore={state:ffCoreState,active:!!ffCore}}loadFFCore();window.__ffCoreBench=(frames=10)=>{let n=33,t=`LUT_3D_SIZE ${n}\n`;for(let b=0;b<n;b++)for(let g=0;g<n;g++)for(let r=0;r<n;r++)t+=`${Math.min(1,Math.pow(r/32,.8)*1.05)} ${g/32*.9+.03} ${Math.pow(b/32,1.2)}\n`;let lut=parseCube(t),clips=state.tracks.flatMap(x=>x.clips).filter(c=>c.asset?.type!=='caption'),saved=clips.map(c=>c.lut);clips.forEach(c=>c.lut=lut);let T=()=>{let t0=performance.now();for(let i=0;i<frames;i++)draw(1.2+i*.04);return (performance.now()-t0)/frames};clips.forEach(c=>c.lut=null);let base=T();clips.forEach(c=>c.lut=lut);let gk=useGlLut;useGlLut=true;let gl=glLut.ready()?T():null;useGlLut=false;let keep=ffCore,z=keep?T():null;ffCore=null;let j=T();ffCore=keep;useGlLut=gk;clips.forEach((c,i)=>c.lut=saved[i]);draw(current);return {canvas:canvas.width+'x'+canvas.height,clips:clips.length,noLutMsPerFrame:+base.toFixed(1),webgl2MsPerFrame:gl&&+gl.toFixed(1),zigMsPerFrame:z&&+z.toFixed(1),jsMsPerFrame:+j.toFixed(1),autoPick:lutPath.pick||null,calibration:lutPath.bench||null}};openDB().then(restoreProject).catch(()=>{});applyProjectFormat(false);renderAll();renderReplaceHistory();$('#clearReplaceHistory').style.display=replaceHistory.length?'block':'none'})();
 
diff --git a/vendor/models/whisper-tiny-en/NOTICE.md b/vendor/models/whisper-tiny-en/NOTICE.md
index e84e234..6a8b492 100644
--- a/vendor/models/whisper-tiny-en/NOTICE.md
+++ b/vendor/models/whisper-tiny-en/NOTICE.md
@@ -7,3 +7,8 @@
 - tokens.json: id-to-token table derived from that revision's vocab.json plus generation settings.
 - FrameForge loads these files only after the user opts in, checks each SHA-256 before use, and runs them on-device with ONNX Runtime Web. No audio leaves the device.
 c0592d0749413c960569e1c7fb806b060d5d18f3ebad4a95cbf9a77dc6e9be52  joined decoder-merged.onnx (cat decoder-merged.onnx.0* > decoder-merged.onnx)
+
+## Optional word aligner (aligner.onnx)
+
+- Source: Xenova/whisper-tiny.en, revision 79fb389fc764e7c395bd330e9531d9d32ada7049, file onnx/decoder_model_quantized.onnx (int8, no key/value cache), shipped as aligner.onnx.0* parts. Base model openai/whisper-tiny.en (MIT per the openai/whisper repository; the Hugging Face card lists apache-2.0).
+- Used only when the user turns on "Keep ums and uhs, with exact word timing". FrameForge runs one extra forward pass per speech window and reads the cross-attention weights of the model's alignment heads to place each word in time. SHA-256 checked before use, cached on the device, never sent anywhere.
